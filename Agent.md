# Agent.md — ClassPulse

Instructions for any coding agent (Claude, Copilot, etc.) working in this repo.

## What this is

Smart QR-Based Real-Time Attendance System. Full spec: [`document.md`](document.md). Phase-by-phase build plan: [`docs/plan.md`](docs/plan.md).

## Tech Stack

Decoupled: Django backend (API + WebSocket only, no server-rendered templates) + separate React SPA frontend.

- **Backend:** Python 3.12, Django, Django REST Framework, Django Channels (WebSocket)
- **DB:** SQLite (dev), PostgreSQL (prod)
- **Frontend:** React + TypeScript or JS, Bootstrap 5 (via `react-bootstrap` or plain Bootstrap CSS), a browser QR-scanner library (e.g. `html5-qrcode`), fetch/axios for REST, native `WebSocket` for the live dashboard
- **Async/QR rotation:** Channels + a periodic task (APScheduler or Celery beat) generating a new `QRToken` every 15s per active session
- **Exports:** `openpyxl` (XLSX), stdlib `csv`, `reportlab`/`weasyprint` (PDF)

## Project Structure (target)

```text
classpulse/
├── backend/
│   ├── manage.py
│   ├── classpulse/       # Django project settings, asgi.py, urls.py
│   ├── accounts/          # Student, Teacher models + auth
│   ├── attendance/         # AttendanceSession, QRToken, Attendance, ActivityLog
│   ├── realtime/            # Channels consumers/routing
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── pages/          # student/, teacher/ views
│   │   ├── components/
│   │   ├── api/             # REST + WebSocket client
│   │   └── App.tsx
│   └── package.json
└── docs/plan.md
```

## Build Order

Follow `docs/plan.md` phases in order (1 → 5). Do not start a phase until the previous phase's exit criteria are met. Each phase is independently demoable.

## Non-Negotiable Rules

1. **Server is the source of truth for QR validity.** The client never decides whether a scan is valid — see doc.md §18/§34. All checks in doc.md §10's validation chain happen in the backend.
2. **Passwords:** never store plaintext. Use Django's built-in password hashing.
3. **Duplicate attendance:** enforce with a DB unique constraint on `(student_id, session_id)`, not application logic alone.
4. **Unique CRN:** enforce with a DB unique constraint, not application logic alone.
5. **QR payload:** an opaque random token only — no student data, no session internals.
6. **No secrets committed.** `DATABASE_URL`, `SECRET_KEY`, etc. go in `.env` / environment, never in code.
7. **CORS/CSRF:** backend is a pure API for a separately-hosted SPA — configure `django-cors-headers` for the frontend origin, use token/session auth appropriately, never disable CSRF protection wholesale.

## Commands (once scaffolded)

```bash
# backend
cd backend
python -m venv venv
venv\Scripts\activate        # Windows (PowerShell)
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver

# frontend
cd frontend
npm install
npm run dev
```

## Conventions

- One Django app per bounded concern (`accounts`, `attendance`, `realtime`) — don't dump everything into one app.
- DRF serializers for all API I/O; validate at the serializer/model layer, not just in views.
- Keep WebSocket consumer logic thin — business logic (validation chain, marking attendance) lives in `attendance/services.py` or similar, callable from both the REST view and the consumer.
- Every new model needs an admin registration for manual inspection during dev.
- Frontend calls backend only through `frontend/src/api/` — no inline `fetch`/`axios` calls scattered in components.
- Bootstrap for layout/components; avoid mixing in another CSS framework.

## Out of Scope (v1)

Multiple subjects/teachers, timetable integration, geofencing, face/device verification, mobile app. See doc.md §4 and §33 for the full future-enhancement list — don't build these ahead of being asked.

## Work Log

Every phase/work update gets an entry here, newest first.

- **2026-08-14** — Day-wise attendance view, in response to "how do I check day/date-wise attendance for a section" — previously the only day-level breakdown was buried in the CSV/Excel/PDF export columns, no on-screen view. Added `get_day_attendance(section, date)` service (every student in the section, present if they attended ANY of that section's sessions on that date — usually one, but stays correct if a teacher starts an extra ad-hoc session same day) and `GET /api/attendance/day/?section=&date=` (`DayAttendanceView`, teacher-only, 400s on missing/malformed params). New `DayAttendancePage` at `/teacher/day-attendance`: section + date pickers, present/absent table with S.No/CRN/Roll No./Name, "No session this day" badge when nothing was started. Also made the app's top nav `position: sticky` (was static, scrolled away) per direct request, and added the new "Day-wise" nav link alongside it. Verified live in-browser: page loads real Section D roster, correctly shows "No session this day" for today (none started yet) with all 60 students marked absent, and the sticky nav stays pinned while scrolling through the table. 8 new backend tests. Full suite: 120/120 passing.
- **2026-08-14** — Analytics table: student search, S.No, and Roll No. columns, plus a real bug fix hit while touching this code. Search box filters the already-fetched student list client-side by name or CRN (no extra API round trip); S.No is derived from the *filtered* array's index, not the original list, so it stays 1, 2, 3... correctly renumbered after a search narrows the results (verified live — searching "Pragya" against 190 students correctly renumbers to 1, 2, not the original row numbers). Added a Roll No. column using `student.username`, since that's the actual URN for every imported student (`StudentProfile` has no separate roll-number field — CRN and URN are different numbers on the real roster, and only CRN was shown before). While adding `roll_number` to `AnalyticsView`, noticed `overall_rate` was computed as `total_students * total_sessions` (a raw session **count**) instead of the periods-weighted total already used for every individual student's own percentage — so the school-wide rate silently drifted low whenever a merged double-period session existed, even though each student's own row was already correct. Fixed to weight by `sum(session.periods for session in sessions)`, matching `build_attendance_matrix()`. 2 new backend tests (roll number present, weighted overall-rate arithmetic on a fixture with one merged and one single-period session). Full suite: 112/112 passing.
- **2026-08-14** — Corrected the training subject: it's always "AI Training", not the generic "Training II" placeholder guessed earlier from the timetable image. Changed `ClassSchedule.subject`'s default, added a data migration (`0013`) renaming the 30 already-seeded rows, and made Start Attendance's subject field a fixed, disabled `"AI Training"` constant instead of free text — since it never varies, there's nothing for a teacher to type or typo, and it can't drift from what the timetable/reports expect. Backend itself stays flexible (any subject string is still accepted at the API level; only the UI fixed it), so this doesn't break the general-purpose `session-start` endpoint or its existing tests. Full suite: 110/110 passing.
- **2026-08-13** — Wrong-section attempts now actively notify the teacher, not just sit passively in the suspicious-activity panel. Previously `wrong_section` events (added in the section-lock fix above) only appeared if the teacher happened to be looking at the panel; the existing toast pattern was success-only ("X marked present"). Added a `toastVariant` ("success" | "warning") to `LiveQRPage` so the same toast mechanism now also fires — in real time over the existing WebSocket `activity.update` channel, no new polling — for wrong-section scans: `"{student} scanned this QR but isn't in Section {X}"`, shown longer (5s vs 3s) and in a warning color. `SessionLiveView` now also returns the session's own `section` so the toast can name the expected section. 1 new backend test confirming the WS broadcast fires for `wrong_section` the same way it already did for `expired_token`. Full suite: 110/110 passing.
- **2026-08-13** — Section lock on attendance marking, in response to the user asking what happens if a wrong-section student scans a section-specific QR — answer was "nothing, they're silently marked present," which is a real gap now that sessions carry a `section` (added for the clickable-timetable feature). Added `WrongSectionError` + `ActivityLog.TYPE_WRONG_SECTION`; `mark_attendance()` now rejects with `"This QR code is for Section A only."` when the scanning student's `StudentProfile.section` doesn't match `session.section` — checked right after the session-status check, before token-expiry/duplicate checks, since being the wrong student for this session is more fundamental than token freshness. Sessions with no section set (started without picking a timetable slot) skip the check entirely — deliberate backward-compat escape hatch, not a loophole, since those sessions were never section-scoped in the first place. Surfaced on the teacher's live suspicious-activity panel as "Wrong section". 3 new tests (wrong section rejected + logged, correct section allowed, section-less session still open to anyone). Full suite: 109/109 passing.
- **2026-08-13** — Clickable timetable: teacher's welcome-page timetable rows are now actionable, not just informational. Added a `section` field to `AttendanceSession` (migration `0010`) so a started session can be reliably matched back to the timetable slot it came from (subject text is free-form and teacher-editable, so matching on it wasn't safe — section is stable and, per the real BBA III timetable, unique per day). `get_today_sessions_by_section(teacher)` looks up today's sessions keyed by section; `TodayScheduleView` now annotates each merged block with `session_id`/`session_status` (only for that request's own user — a student never sees another user's session, and gets nulls throughout since students don't have sessions). Clicking a row with no session navigates to Start Attendance pre-filled (subject, section, and the merge checkbox pre-checked for double periods) via router state; clicking a row that already has a session jumps straight to its live view (`LiveQRPage`) — "View live" while active, "View attendance" once closed — so a teacher can see who's marked present without hunting through Analytics. Verified the full loop live in-browser via Playwright: click unscheduled slot → prefilled Start Attendance form (merge box correctly pre-checked for the D double period) → start session → QR/live view → back to welcome page → click same slot again → lands back on the *same* running session (countdown continued, not reset). 8 new backend tests (session-linking by section, isolation between teachers). Full suite: 106/106 passing.
- **2026-08-13** — Merge-attendance option for double periods: previously merging was display-only (Agent.md entry above) — a teacher still had to run two separate QR sessions for a continuous double period, so a student had to scan twice. Added a `periods` field (1 or 2) to `AttendanceSession` (migration `0009`), exposed on `StartSessionSerializer`/`SessionSerializer`. `StartAttendancePage` gained a "Merge with next continuous period" checkbox — checking it sends `periods: 2`, so one QR session and one scan covers both timetable slots. Attendance weighting updated everywhere percentages are computed so a merged session counts as 2 towards both present and total: `build_attendance_matrix()` (teacher analytics/exports) and `StudentHistoryView` (student's own history) now sum `session.periods` instead of counting sessions 1-for-1 — kept both in sync since they'd silently diverge otherwise. CSV/Excel/PDF export headers mark merged sessions as `"YYYY-MM-DD (x2)"` so a teacher reading the raw export isn't confused by the weight. 6 new tests (start with `periods=2`, reject `periods=3`, merged-session doubles both numerator and denominator in the matrix). Full suite: 103/103 passing.
- **2026-08-13** — Merge back-to-back same-section timetable periods into one display block, per user pointing out that consecutive slots for the same section (e.g. 10:05-10:55 and 11:05-11:55, both BBA III D) are really one continuous double-period class. Added `merge_consecutive_slots()` in `services.py`: walks the day's slots in order, folding a slot into the previous block when section+subject match — deliberately merges across the real timetable's short between-period break (10:55 → 11:05) rather than requiring an exact zero-gap boundary, since that's what the actual printed schedule looks like. `TodayScheduleView` now returns merged blocks, so both welcome-page timetable cards show "10:05 AM - 11:55 AM" once instead of two rows. 5 new tests (merge across a gap, no merge across a different section, no merge for different section back-to-back, full real Thursday schedule collapsing 6 rows to 3). Full suite: 99/99 passing.
- **2026-08-13** — Today's timetable on both welcome pages: added `get_today_schedule()` service + `GET /api/attendance/schedule/today/` (any authenticated role) returning all of today's slots in order, plus the weekday name. Teacher's welcome page (`TeacherProfilePage`) shows every slot for today across all sections; student's welcome page (`StudentProfilePage`) shows the same feed filtered client-side to the student's own `section`. Both cards are silent-fail (timetable is a convenience, not required for the rest of the dashboard to work) and hide entirely if there's nothing to show. New `formatTime()` util renders the backend's `"HH:MM:SS"` `TimeField` strings as a 12-hour clock. Verified live in-browser via Playwright screenshot on the actual running dev servers (not just tests) — logging in as `admin` on the real current day (Thursday) correctly showed only the D/D/E/E/F/F Thursday slots; logging in as a Section D student correctly showed only that section's 2 Thursday slots. 3 new backend tests (today-schedule service ordering, teacher sees all sections, student can access too). Full suite: 94/94 passing.
- **2026-08-13** — Timetable auto-fill for Start Attendance: added a `ClassSchedule` model (day of week, start/end time, section, subject) seeded via data migration with the real BBA III (Training II) weekly timetable from the printed schedule — 30 slots across Mon-Fri, sections A-F, Lab 3E. New `GET /api/attendance/schedule/current/` (teacher-only) resolves the slot covering the current local time via `get_current_schedule_slot()`; returns `{matched: false}` outside any slot (end time is exclusive, so back-to-back slots don't double-match). `StartAttendancePage` calls it on mount and pre-fills the subject field (`"Training II — BBA III D"`) with a visible "Auto-filled from today's timetable" hint when matched; silently no-ops otherwise since it's a convenience, not a requirement. `ClassSchedule` registered in Django admin for manual edits if the timetable changes next semester. 6 new tests (slot-matching edge cases including the exclusive end-time boundary, view auth, student-forbidden). Full suite: 91/91 passing.
- **2026-08-13** — Switched DB engine from SQLite to MySQL-family per explicit user request (`DB_NAME=classpulse, DB_USER=root, DB_PASSWORD=`). Discovered two local MySQL-compatible services already installed: a XAMPP-bundled MariaDB 10.4.32 (port 3306, root/no-password — matched the requested credentials exactly) and a standalone MySQL 8.0 (port 3307, root password set). Django 5.2's MySQL backend hard-rejects anything below MariaDB 10.5 (`NotSupportedError` at connection time), so 10.4.32 was unusable as-is. Rather than touch the XAMPP install in place (risk to other XAMPP-dependent projects) or reset MySQL 8.0's root password (declined), installed a separate MariaDB 12.3.2 via `winget install MariaDB.Server`, registered it as its own Windows service (`MariaDB`) on port 3306 — XAMPP's `mysql` service stopped (not removed) to free the port. Root account needed an explicit `ALTER USER ... IDENTIFIED VIA mysql_native_password USING ''` — the installer's default empty-plugin/empty-password root account authenticated fine via the `mysql` CLI but was rejected by PyMySQL's auth handshake. Added `pymysql.install_as_MySQLdb()` shim to `backend/classpulse/__init__.py` (PyMySQL chosen over `mysqlclient` to avoid needing a C build toolchain on Windows), pointed `DATABASES` in `settings.py` at `classpulse` (utf8mb4/utf8mb4_unicode_ci) on `127.0.0.1:3306`. Fresh-start migration (per user's choice, not a data migration): ran `migrate` clean, recreated the `admin` superuser (`Admin@1234`, role=teacher), re-ran the CSV import for the 190 students from `Data/Section D/E/F.csv` (same scheme as the original import: username=password=URN, course=BBA, semester=1, section per file). Full suite: 85/85 passing against MariaDB. Added `PyMySQL==1.2.0` to `requirements.txt`. Old `db.sqlite3` left on disk, untouched and unused.
- **2026-08-11** — Phase 4 Task 7 done (final task): suspicious-activity panel on `LiveQRPage` — initial REST fetch + live WebSocket feed, discriminated from attendance events via a `kind` field. Spec review confirmed none of this file's 3 prior review-caught bug classes (unhandled poll failure, stale error, dead WS with no feedback) were regressed. Code-quality review caught a NEW instance of the same "does every path degrade gracefully" failure class: an unmapped `activity_type` from the backend would crash the whole dashboard render (`ACTIVITY_LABELS[type]` → `undefined` → property access throws), and a failed activity fetch was silently swallowed — on a *security* panel, an empty list reading as "checked, nothing found" instead of "couldn't check" is actively misleading. Fixed both: defensive fallback label, and a visible warning on fetch failure. **Phase 4 (Security & Suspicious Activity) complete** — all 7 tasks done, exit criteria met (every attempt logged with IP/device, duplicate/expired attempts visible to teacher near-real-time, new-device heuristic in place, Phase 2's DB-level duplicate/expiry enforcement untouched).
- **2026-08-11** — Phase 4 Task 6 done: `ActivityLog` registered in admin, backend sanity check — 60/60 tests passing, `manage.py check` clean. Backend half of Phase 4 complete (Tasks 1-6) — moving to frontend (Task 7).
- **2026-08-11** — Phase 4 Task 5 done: `GET /api/attendance/sessions/<id>/activity/` (teacher-only, ownership-scoped) — last 50 non-success activity entries, for initial dashboard load. Spec + code-quality reviewed, approved. Fixed one gap before merge: relied on `ActivityLog.Meta.ordering` implicitly instead of an explicit `.order_by()` (would silently break if that default ever changed) — made explicit, matching `SessionLiveView`'s established style. Noted, not fixed: no test locks down the 50-row cap itself.
- **2026-08-11** — Phase 4 Task 4 done: `activity_update` consumer handler relays suspicious-activity broadcasts to connected teacher dashboards — closes a gap that existed since Task 2 (broadcasts were being sent since then but had no handler to receive them on a real, non-mocked connection). Verified directly (exact-match spec, 3-line additive diff).
- **2026-08-11** — Phase 4 Task 3 done: new-device detection — logs `TYPE_NEW_DEVICE` (non-blocking) when a student's User-Agent changes between successful marks. Spec + code-quality reviewed, approved. Flagged for later (not fixed, both non-blocking): exact-string UA comparison will likely false-positive often in real use (browser auto-updates, extensions) — fine for a v1 "log it" heuristic, but shouldn't drive any real alert/notification without normalization first; no DB index on `(student, activity_type, created_at)` for this now-per-mark query, negligible at current scale.
- **2026-08-11** — Phase 4 Task 2 done: mark-attendance validation moved from Phase 2's DRF serializer into `services.py::mark_attendance()`, exception-driven (5 typed exceptions from Task 1), every branch logs an `ActivityLog` row before failing/succeeding. Spec + code-quality reviewed, approved. Fixed one real gap before merge: `log_activity()`'s DB write was unguarded — a transient failure there could turn an already-successful, already-committed attendance mark into a 500 for the student (same failure class as the Phase 3 broadcast fix); wrapped in try/except, logs and continues. Noted, not fixed: double duplicate-check (pre-check + IntegrityError backstop) is intentional TOCTOU-safe redundancy but undocumented; exception classes couple internal `activity_type` with user-facing `message` on one attribute set.
- **2026-08-12** — Phase 5 Task 9 done (final task): teacher `AnalyticsPage` — totals, overall rate, per-student table, below-threshold warning, CSV/Excel/PDF export buttons via blob download. Spec + code-quality reviewed. Needs-changes on first pass, both real: (1) export buttons had no disabled/in-progress state, so a double-click could fire two concurrent downloads with no feedback; (2) the 75% threshold was now hardcoded independently in 3 places (backend `AnalyticsView`, `AttendanceHistoryPage`, this page) — the "fix on third occurrence" bar flagged in an earlier review had been hit. Fixed: added a `downloadingFormat` guard disabling all three buttons mid-download; extracted `ATTENDANCE_THRESHOLD` into `api/client.ts`, used by both frontend pages (backend's own `75` literal in `AnalyticsView` still separate — cross-language, no shared-constant mechanism exists). Also hardened `revokeObjectURL` timing for Safari/WebKit (delayed via `setTimeout` instead of firing synchronously after `click()`). Re-verified directly, approved.

- **2026-08-13** — Two feature additions on user request. (1) Section filter on teacher analytics + exports: `build_attendance_matrix(section=)` now optionally scopes students; `AnalyticsView`/`ExportCSVView`/`ExportExcelView`/`ExportPDFView` all accept `?section=`; `available_sections` + current `section` echoed in the analytics response for the frontend dropdown; export filenames include the section when filtered. (2) Configurable attendance-window duration (doc.md §12): `AttendanceSession.duration_minutes` (default 5, 1-180) + `closes_at` computed property; `close_session_if_window_expired()` lazily auto-closes on the next touch (mark attendance, QR fetch, live poll) — same no-scheduler-needed pattern as QR-token rotation. Teacher picks the window on Start Attendance; live dashboard shows a client-side countdown against `closes_at`. Also: bulk-imported 190 real students from user-supplied section CSVs (D/E/F, BBA sem 1, username=password=URN) via a one-off script, not committed to git (`Data/` added to `.gitignore` — PII); added `list_display`/`list_filter`/`search_fields` to Django admin's `StudentProfile` registration so section-wise lookup doesn't require a script; fixed a real crash (`StudentProfile.DoesNotExist` → unhandled 500, flagged back in Phase 1 and never fixed) triggered when the `admin` Django-admin-only account was used to log into the app — now a clean 404, with a regression test.
- **2026-08-13** — Full visual redesign, all 9 pages. Root cause of the reported invisible-login-heading bug: `index.css` was still the unmodified Vite scaffold template — `color-scheme: light dark` + a dark-mode `prefers-color-scheme` variant set heading color near-white, invisible on Bootstrap's white background. Replaced entirely with a real design system: chalkboard-green + brass + paper-cream palette, Space Grotesk/Inter/IBM Plex Mono, a "stamp" badge as the signature element (present/absent/status, styled as a tilted rubber ink-stamp — fits the attendance-register subject directly) replacing every plain Bootstrap Badge. Added `AppShell` — a shared chalkboard header with role-aware nav and a working Log Out control, which did not exist anywhere in the UI before this. Wired in the user-provided ClassPulse logo (resized 1254px→256px, 628KB→52KB, since it only ever renders at 40-64px). `LiveQRPage`/`AnalyticsPage` got visual-only changes — every state/effect/handler left byte-for-byte untouched given this file's multi-round bug history. Picked up two small fixes along the way: `controlId` on all form groups (a11y gap flagged back in Phase 1, never fixed), semester input min/max matching backend validation. Verified via Playwright screenshots against live servers (login, register, teacher profile+shell, analytics) — all render correctly. Backend untouched, 77/77 still passing.
- **2026-08-13** — Manual E2E verification run: real browser (Playwright/Chromium), both dev servers live, full flow — student register → login → teacher login → start session → live QR → mark attendance (via API, camera unavailable headless) → real-time WebSocket update on teacher dashboard (toast + present count + recent list, confirmed via screenshot) → stop session → student history page (correct 100%, closed-sessions-only) → teacher analytics page (correct aggregation) → CSV export download triggered successfully. Zero console/page errors across all 7 pages visited. **Nothing broken found** — this was the first real click-through since the whole build; every phase held up. Test accounts and scratch script cleaned up afterward; `playwright` devDependency added only for this run, reverted (not part of the app's actual stack).

**🎉 ClassPulse v1 complete.** All 5 phases from `docs/plan.md`'s Development Plan are built, reviewed, and committed: Authentication → Attendance Sessions & QR → Real-Time Dashboard → Security & Suspicious Activity → Reports & Analytics. Every task went through two-stage review (spec compliance + code quality); real bugs were caught and fixed at nearly every stage, several more than once on the same file (`LiveQRPage.tsx`: 4 rounds; PDF export column math: 2 rounds). Backend: 79 tests passing. Frontend: builds clean. Not yet done: a manual end-to-end browser walkthrough (register → login → start session → scan → live dashboard → close session → history/analytics/export) — only automated tests and build checks have been run so far.
- **2026-08-12** — Phase 5 Task 8 done: student `AttendanceHistoryPage` — total/present/percentage badge + full history table, linked from `StudentProfilePage`. Spec + code-quality reviewed, approved. Flagged (not fixed, explicit follow-up): the fetch-failure pattern (logout + redirect on ANY error, not just 401/403) is now duplicated across 3 pages (student profile, teacher profile, this one) — a transient network blip destroys a valid session same as an auth failure. Worth a shared `handleAuthOrRedirect()` helper before a 4th page copies it. Minor, not fixed: no "no history yet" empty state for brand-new students.
- **2026-08-12** — Phase 5 Task 7 done: backend sanity check — 77/77 tests passing, `manage.py check` clean. Backend half of Phase 5 complete (Tasks 1-7: matrix service, student history, teacher analytics, CSV/Excel/PDF export) — moving to frontend (Tasks 8-9).
- **2026-08-12** — Phase 5 Task 6 done: `GET /api/attendance/export/pdf/` (teacher-only) — final export format, via `reportlab`, reusing `build_report_rows()`. Code-quality review reproduced 3 real defects, all verified with actual numbers: (1) unconstrained `Table` overflowed the page ~4.7x at realistic scale (45 students × 60 sessions), silently clipping most of the report; (2) reusing the CSV/Excel sanitizer's quote-prefix in PDF showed a stray literal apostrophe (PDF isn't a spreadsheet, that convention doesn't apply); (3) no `repeatRows`, so multi-page reports lost headers after page 1. First fix pass addressed (2) and (3) correctly but got (1) wrong — margin miscalculated (off by 72pt) and a "minimum floor" that itself pushed total width past the page once column count passed ~24, reproduced concretely by the reviewer down to specific overflow ratios; the fix's own weak test (`len(pages) >= 1`) couldn't have caught this since reportlab clips silently rather than erroring. Second pass: corrected the margin, removed the overflow-causing floor (columns now shrink toward unreadable rather than ever exceeding page width), extracted the width math into a standalone `compute_pdf_column_widths()` and tested the actual invariant directly (`sum(widths) <= usable_width`, parametrized up to 200 columns) instead of trying to infer it from rendered PDF geometry. Verified directly this time, not re-reviewed by agent — math is transparent and unit-tested.
- **2026-08-12** — Phase 5 Task 5 done: `GET /api/attendance/export/excel/` (teacher-only) — XLSX via `openpyxl`, deliberately deviated from the plan's original inline code to reuse `build_report_rows()` instead, so it inherits the CSV export's formula-injection sanitization for free rather than re-duplicating it. Spec + code-quality reviewed, approved. Added an Excel-specific sanitization regression test to close a coverage gap the review flagged (CSV had one, Excel didn't). Noted, not fixed (cosmetic only): `openpyxl` has no native "quote prefix" cell attribute, so the sanitizing `'` prefix is safe (never evaluated as a formula) but displays as a literal leading apostrophe in Excel, unlike CSV where spreadsheet apps typically absorb it on import.
- **2026-08-12** — Phase 5 Task 4 done: `GET /api/attendance/export/csv/` (teacher-only) — downloadable CRN×Name×session-dates×% matrix. Code-quality review found a real security gap: CRN/name are free-text at student self-registration, and were written unescaped into CSV cells — a student registering with a name/CRN starting with `=`/`+`/`-`/`@` gets formula-injected into any teacher who opens the export in Excel/Sheets (CWE-1236). Fixed with `sanitize_report_cell()` (quote-prefix guard), extracted alongside a shared `build_report_rows()` helper so the fix — and the row-building logic itself — doesn't get re-duplicated when Excel/PDF exports land in Tasks 5-6. Also added a UTF-8 BOM so Excel renders non-ASCII student names correctly, and a regression test locking down the sanitization. Re-reviewed, approved.
- **2026-08-12** — Phase 5 Task 3 done: `GET /api/attendance/analytics/` (teacher-only) — overall rate, per-student breakdown, below-threshold list. Spec + code-quality reviewed, approved. Fixed: `overall_rate` had already redundantly duplicated the percentage formula inline instead of using `attendance_percentage()` extracted one commit earlier — deduped. Noted, not fixed: `75` threshold is a bare literal (fine for v1, worth a named constant if it ever becomes configurable); endpoint has no per-teacher ownership scoping — deliberate v1 limitation (single-class scope, no course model to scope by), documented as such, would need revisiting for multi-teacher support.
- **2026-08-12** — Phase 5 Task 2 done: `GET /api/attendance/student/history/` — student's own history + percentage, closed sessions only. Implementer caught and fixed a real bug in this plan's own test fixture (two sessions sharing a subject broke a dict-keyed assertion) — verified minimal and correctly scoped to the test, not the view. Spec + code-quality reviewed, approved. Fixed proactively: extracted `attendance_percentage()` shared helper (percentage formula was independently duplicated in `views.py` and `services.py`, would have become a third duplicate at Task 3/analytics) — same formula, one place now.
- **2026-08-12** — Phase 5 (Reports & Analytics) started. Wrote detailed plan: `docs/superpowers/plans/2026-08-12-phase-5-reports.md` (9 tasks). Task 1 done: shared `build_attendance_matrix()` service — closed sessions × students × present/absent grid, reused by student history, teacher analytics, and CSV/Excel/PDF exports. Spec + code-quality reviewed, approved. Fixed proactively (not a bug, a maintainability call): return value changed from a bare `(sessions, rows)` tuple to a `NamedTuple` before 5 planned callers land — same unpacking syntax, more self-documenting. Flagged, not fixed: CRN sort is lexicographic not numeric (fine if CRNs are zero-padded in practice; worth checking real data before it flows into report exports).
- **2026-08-11** — Phase 4 (Security & Suspicious Activity) started. Wrote detailed plan: `docs/superpowers/plans/2026-08-11-phase-4-security.md` (7 tasks). Task 1 done: `ActivityLog` model (audits every attendance attempt) + `attendance/exceptions.py` (5 typed failure classes, wired up in Task 2). Spec + code-quality reviewed, approved; fixed one real gap before it went live: `session` FK was `CASCADE` (deleting a session would wipe its own audit trail) — changed to `SET_NULL` so log entries survive. Also noted for later, not fixed: `AttendanceError` base class doubles as a usable concrete exception (should be raised only via subclasses; harmless today since nothing raises the base directly).
- **2026-08-11** — Phase 3 Task 8 done (final task): `LiveQRPage` extended with present-count badge, recent list, toast notifications, live-driven by `connectToAttendanceSocket`. Code-quality review found the exact "silent stall, no feedback" pattern this file was already fixed for twice in Phase 2 — reintroduced via unhandled `getSessionLive` rejection and a WebSocket with no error/close handling or reconnect; fixed with `.catch()`, `onerror`/`onclose`/auto-reconnect in `ws.ts`, a status badge, and a stable per-event toast key (also found + fixed: unstable list `key`, and a toast-autohide-timer race where same-named students could dismiss each other's toast early). Re-review caught the toast key fix was incomplete (message-text key still collided for same-named students); tightened to a monotonic counter. Re-verified directly, approved. **Phase 3 (Real-Time Dashboard) complete** — all 8 tasks done, exit criteria met (WebSocket push updates dashboard with no refresh, auth+ownership scoped per session, QR rotation unaffected). Known accepted tradeoff: WebSocket reconnects forever with no backoff/cap if the endpoint is fundamentally broken — flagged as non-blocking follow-up for Phase 4+.
- **2026-08-11** — Phase 3 Task 7 done: frontend `getSessionLive` REST call + `connectToAttendanceSocket` native WebSocket client (token via query string, matches localStorage token). Verified directly (exact-match spec, pure additions).
- **2026-08-11** — Phase 3 Task 6 done: backend sanity check — 49/49 tests passing, `manage.py check` clean, dev server confirmed serving via daphne (`Server: daphne` header, not plain WSGI dev server). Backend half of Phase 3 complete (Tasks 1-6) — moving to frontend (Tasks 7-8).
- **2026-08-11** — Phase 3 Task 5 done: `GET /api/attendance/sessions/<id>/live/` (teacher-only, ownership-scoped) — present count + last-10 recent list, for initial dashboard load before WS events arrive. Spec + code-quality reviewed, approved. Noted for frontend Task 8: WS event payload and REST `recent` items share `{name, crn, marked_at}` shape but `present_count` nests differently (per-event in WS vs. top-level in REST) — plan's Task 8 code already handles this correctly by destructuring explicitly, not reusing types wholesale.
- **2026-08-11** — Phase 3 Task 4 done: `broadcast_attendance_update` called from `MarkAttendanceView` after successful mark, pushes `{name, crn, marked_at, present_count}` to session's WS group. Code-quality review found reliability gap: unguarded `group_send` could turn a successful, already-committed DB write into a 500 for the student; fixed with try/except + logging (never masks a successful mark), also tidied mid-file imports. Re-verified directly, approved.
- **2026-08-11** — Phase 3 Task 3 done: `AttendanceConsumer` — teacher-only, ownership-checked (close 4403/4404), joins `attendance_session_<id>` group. Code-quality review found fragile `disconnect()` guard (`hasattr(self, "group_name")` doesn't actually track join state, since `group_name` is set before auth checks run); fixed with explicit `joined_group` flag. Re-verified directly, approved.
- **2026-08-11** — Phase 3 Task 2 done: `TokenAuthMiddleware` (WebSocket auth, resolves `?token=` against the existing DRF `authtoken.Token`, sets `scope["user"]`). Code-quality review found deactivated users could still authenticate over WS (no `is_active` check, unlike DRF's own `TokenAuthentication`); fixed with `user__is_active=True` filter. Re-verified directly, approved.
- **2026-08-11** — Phase 3 (Real-Time Dashboard) started. Wrote detailed plan: `docs/superpowers/plans/2026-08-11-phase-3-realtime-dashboard.md` (8 tasks). Task 1 done: Django Channels scaffolded (`daphne`+`channels` in `INSTALLED_APPS`, in-memory channel layer, `asgi.py` rewritten). Spec + code-quality reviewed, approved; added a one-line settings comment on the in-memory layer's single-process constraint per review.
- **2026-08-11** — Phase 2 Task 9 done (final task): student `ScanQRPage` (camera scan via `html5-qrcode`, auto-submits to `markAttendance`), linked from `StudentProfilePage`. Code-quality review found Critical bug (`scanner.stop()` throws synchronously, not via rejected promise, if camera permission was denied — escaped the `.catch()` guard uncaught) plus the same missing-mounted-guard pattern already fixed twice on `LiveQRPage`; fixed with `getState() === SCANNING` check before stop + `active` flag. Re-reviewed, approved. **Phase 2 (Attendance Sessions & QR) complete** — all 9 tasks done, exit criteria met (QR rotates 15s, one scan = one mark, DB-level duplicate prevention, all validation server-side).
- **2026-08-11** — Phase 2 Task 8 done: teacher `StartAttendancePage` + `LiveQRPage` (polls QR every 15s via `qrcode.react`), linked from `TeacherProfilePage`. Code-quality review found two Critical bugs repeating a Phase-1-fixed pattern (unhandled poll failure → stuck spinner forever; unhandled stop failure → button silently no-ops) — fixed with error state + unconditional navigate-on-stop. Re-review caught a third bug in the fix itself (error state never cleared on recovery, permanently hiding QR after one transient blip) — fixed with `setError(null)` on successful poll. Verified directly, approved.
- **2026-08-11** — Phase 2 Task 7 done: frontend API client extended (`startSession`, `stopSession`, `getSessionQR`, `markAttendance`). Verified directly (exact-match spec, pure append) — no separate review dispatch needed for this mechanical task.
- **2026-08-11** — Phase 2 Task 6 done: attendance models registered in admin. Full backend suite: 35/35 passing. Backend half of Phase 2 complete (Tasks 1-6) — moving to frontend (Tasks 7-9).
- **2026-08-11** — Phase 2 Task 5 done: `POST /api/attendance/mark/` — core feature, full server-side validation chain (token exists → session active → not expired → not already marked → student role via `IsStudent`), student-only, DB-level `UniqueConstraint` backstop. Code-quality review found Critical bug: unhandled `IntegrityError` under concurrent duplicate-scan race → raw 500 instead of clean 400; fixed with `transaction.atomic()` + `except IntegrityError` in the view. Re-reviewed, approved. Follow-up nice-to-have (not blocking): a mocked test exercising the `except IntegrityError` branch directly, vs. current code-inspection-level confidence — true threading-based race test out of scope.
- **2026-08-11** — Phase 2 Task 4 done: `GET /api/attendance/sessions/<id>/qr/` (teacher-only, ownership-scoped), lazy 15s rotation via `attendance/services.py::get_current_qr_token`. Code-quality review found non-deterministic tie in token ordering (`order_by("-created_at")` alone); fixed with `-id` secondary sort. Confirmed benign, accepted TOCTOU race on concurrent-request duplicate-mint (no security/correctness impact — validation at scan time is fully decoupled from issuance, per `MarkAttendanceSerializer` design in Task 5).
- **2026-08-11** — Phase 2 Task 3 done: teacher-only `POST /api/attendance/sessions/start/` and `.../stop/` (ownership-scoped, other teachers' sessions 404). Code-quality review found repeat-stop-call bug (`end_time` silently overwritten on each call); fixed with an already-closed guard returning 400. Re-reviewed, approved.
- **2026-08-11** — Phase 2 Task 2 done: `attendance` app scaffolded, `AttendanceSession`/`QRToken`/`Attendance` models (DB-level `UniqueConstraint` on student+session). Code-quality review found latent bug (`QRToken.expires_at` NOT NULL but only set in `save()` override — bypassable via bulk_create/full_clean); fixed by switching to `default=` callable, matching the `token` field's existing pattern. Re-reviewed, approved.
- **2026-08-11** — Phase 2 (Attendance Sessions & QR) started. Wrote detailed plan: `docs/superpowers/plans/2026-08-11-phase-2-attendance-qr.md` (9 tasks). Task 1 done: `IsTeacher`/`IsStudent` DRF permission classes (`accounts/permissions.py`) — closes the role-gating gap flagged at end of Phase 1. Spec + code-quality reviewed, approved. Note: permission tests exercise `has_permission` directly rather than through a real DRF request/view cycle — acceptable, integration coverage arrives naturally once Task 3+ wires these into `attendance` views.
- **2026-08-10** — Phase 1 Task 9 done (final task): teacher profile endpoint (`GET /api/teacher/profile/`) + `TeacherProfilePage`, symmetric to student profile. Spec + code-quality reviewed, approved. **Phase 1 (Authentication & Profiles) complete** — all 9 tasks done, exit criteria met (student register/login/profile, teacher login/profile, DB-level unique CRN). Cross-cutting gap flagged for Phase 2: neither profile endpoint checks `request.user.role` — a student token can fetch `/api/teacher/profile/` and vice versa, no error, just wrong-shaped-but-valid data. Not blocking Phase 1, becomes load-bearing once Phase 2 adds teacher-only session endpoints.
- **2026-08-10** — Phase 1 Task 8 done: registration/login/student-profile pages + route guard (`ProtectedRoute`), wired in App.tsx. Code-quality review found unhandled-401 stuck-spinner bug + blank page on unmatched route (teacher login pre-Task-9); fixed (StudentProfilePage now logs out + redirects on fetch failure, added catch-all route → `/login`). Re-reviewed, approved. Remaining minor/non-blocking: RegisterPage's narrow error-message fallback, missing `controlId` on form groups (a11y), no visually-hidden spinner text.
- **2026-08-10** — Phase 1 Task 7 done: frontend API client (`frontend/src/api/client.ts`) — register/login/profile calls, token interceptor. Spec + code-quality reviewed, approved. Notes for later: type responses on `registerStudent`/`getStudentProfile` (currently `any`), move `BASE_URL` to env var before deploy, no server-side token invalidation on logout.
- **2026-08-10** — Phase 1 Task 6 done: React+Vite+TS frontend scaffolded (`frontend/`), Bootstrap 5 + react-bootstrap + react-router-dom + axios installed. Spec + code-quality reviewed, approved. Note: scaffold resolved React 19 (plan said React 18) — non-breaking, watch for API differences in later tasks.
- **2026-08-10** — Phase 1 Task 5 done: student profile endpoint (`GET /api/student/profile/`), admin registration for User+StudentProfile. Spec + code-quality reviewed, approved with follow-ups: (1) `StudentProfile.DoesNotExist` → unhandled 500 if a teacher or profile-less user hits this route, should be 403/404 — fix before frontend relies on it; (2) `photo` serializes as relative path, not absolute URL (no `context=request` passed); (3) mid-file imports in views.py, cosmetic.
- **2026-08-10** — Phase 1 Task 4 done: role-aware token login (`POST /api/student/login/`, returns token+role+username). Spec + code-quality reviewed, approved. Noted for later phase: no logout/token-revocation endpoint, no throttling.
- **2026-08-10** — Phase 1 Task 3 done: student registration endpoint (`POST /api/student/register/`), DB-level unique CRN + username enforced. Spec + code-quality reviewed, approved. Noted for later: response omits `photo` field, minor TOCTOU race on uniqueness checks under concurrent requests (plan-level tradeoff, not blocking).
- **2026-08-10** — Phase 1 Task 2 done: custom `User` model (role field) + `StudentProfile` (unique CRN) in `accounts` app, migration + 3 model tests passing. Spec + code-quality reviewed, approved.
- **2026-08-10** — Phase 1 Task 1 done: Django backend scaffolded (`backend/classpulse`, `backend/accounts`), DRF token auth + CORS + media settings wired. Spec + code-quality reviewed, approved. Added root `.gitignore`.
- **2026-08-10** — Wrote detailed Phase 1 (Auth) implementation plan: `docs/superpowers/plans/2026-08-10-phase-1-auth.md`. TDD task-by-task, backend (Django+DRF token auth) and frontend (React+Bootstrap). Not yet executed.
- **2026-08-10** — Stack finalized: decoupled Django REST/Channels backend + React+Bootstrap frontend (`backend/` + `frontend/`). Updated `Agent.md`, `docs/plan.md`, `Readme.md`.
- **2026-08-10** — Repo initialized. Wrote `document.md` spec (pre-existing), `docs/plan.md` (5-phase plan), `Agent.md`, `Readme.md`. No code yet.
