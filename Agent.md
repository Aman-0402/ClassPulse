# Agent.md — ClassPulse

Instructions for any coding agent (Claude, Copilot, etc.) working in this repo.

## What this is

Smart QR-Based Real-Time Attendance System. Full spec: [`document.md`](document.md). Phase-by-phase build plan: [`docs/plan.md`](docs/plan.md).

## Tech Stack

Decoupled: Django backend (REST API only, no server-rendered templates) + separate React SPA frontend.

- **Backend:** Python 3.10+ / 3.12, Django, Django REST Framework, WSGI/Passenger-compatible
- **DB:** MySQL (prod on cPanel), SQLite/MySQL-compatible test/dev flows depending on environment
- **Frontend:** React + TypeScript or JS, Bootstrap 5 (via `react-bootstrap` or plain Bootstrap CSS), a browser QR-scanner library (e.g. `html5-qrcode`), axios for REST polling/live dashboard updates
- **Live updates / QR rotation:** REST polling; QR display rotates every 15s, scanned tokens remain accepted for a short grace window
- **Exports:** `openpyxl` (XLSX), stdlib `csv`, `reportlab`/`weasyprint` (PDF)

## Project Structure (target)

```text
classpulse/
├── backend/
│   ├── manage.py
│   ├── classpulse/       # Django project settings, wsgi.py, urls.py
│   ├── accounts/          # Student, Teacher models + auth
│   ├── attendance/         # AttendanceSession, QRToken, Attendance, ActivityLog
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── pages/          # student/, teacher/ views
│   │   ├── components/
│   │   ├── api/             # REST + WebSocket client
│   │   └── App.tsx
│   └── package.json
└── docs/plan.md
```

## Build Order

Follow `docs/plan.md` phases in order (1 → 5). Do not start a phase until the previous phase's exit criteria are met. Each phase is independently demoable.

## Non-Negotiable Rules

1. **Server is the source of truth for QR validity.** The client never decides whether a scan is valid — see doc.md §18/§34. All checks in doc.md §10's validation chain happen in the backend.
2. **Passwords:** never store plaintext. Use Django's built-in password hashing.
3. **Duplicate attendance:** enforce with a DB unique constraint on `(student_id, session_id)`, not application logic alone.
4. **Unique CRN:** enforce with a DB unique constraint, not application logic alone.
5. **QR payload:** a frontend scan URL containing only an opaque random token in `?token=` — no student data, no session internals.
6. **No secrets committed.** `DATABASE_URL`, `SECRET_KEY`, etc. go in `.env` / environment, never in code.
7. **CORS/CSRF:** backend is a pure API for a separately-hosted SPA — configure `django-cors-headers` for the frontend origin, use token/session auth appropriately, never disable CSRF protection wholesale.
8. **Never bulk-touch `StudentProfile.photo` (or any other student data field) in the database without the user explicitly asking for that specific change, that day.** On 2026-09-23, `scripts/find_missing_photos.py --fix` was run and correctly cleared 193 students' `photo` field because the actual image files were missing from the server's `media/` folder — a real, justified fix for a real problem, not a mistake in the script itself. But this class of action — a script that writes to every row of student data at once — is exactly the kind of change that must never happen quietly or by default: don't write a script that mutates student photo/profile data in bulk unless the user's current message asks for that specific fix, and always run it read-only first (no `--fix`/write flag) and show the user what it would change before it changes anything. Never add a "cleanup" or "fix stale data" step to a routine deploy script that touches student photo/profile rows automatically — a media folder going empty (host issue, accidental cPanel File Manager delete, bad restore, wrong path) must surface as a loud, investigated problem, never something a script silently launders by clearing the DB references. See `scripts/find_missing_photos.py`'s own read-only-by-default design and the `Avatar` component's `onError` fallback (`frontend/src/components/Avatar.tsx`) — that fallback is the correct place to hide a broken image in the UI; clearing the DB is a separate, deliberate, user-approved step, not something to do proactively again.

## Commands (once scaffolded)

```bash
# backend
cd backend
python -m venv venv
venv\Scripts\activate        # Windows (PowerShell)
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver

# frontend
cd frontend
npm install
npm run dev
```

## Conventions

- One Django app per bounded concern (`accounts`, `attendance`, etc.) — don't dump everything into one app.
- DRF serializers for all API I/O; validate at the serializer/model layer, not just in views.
- Keep view logic thin — business logic (validation chain, marking attendance) lives in `attendance/services.py` or similar, callable from multiple API views.
- Every new model needs an admin registration for manual inspection during dev.
- Frontend calls backend only through `frontend/src/api/` — no inline `fetch`/`axios` calls scattered in components.
- Bootstrap for layout/components; avoid mixing in another CSS framework.

## Out of Scope (v1)

Multiple subjects/teachers, timetable integration, geofencing, face/device verification, mobile app. See doc.md §4 and §33 for the full future-enhancement list — don't build these ahead of being asked.

## Phase 6 Plan: Tasks + Exam System

Two new features, spec locked in with the user 2026-09-24. Each phase gets its
own Work Log entry when done; this section is the standing plan, updated only
if scope changes.

**Tasks** (view-only, per section, no submission):
- Admin/teacher posts a task (title, description, due date) to one section.
- Every student in that section sees it, read-only, soonest-due first.

**Exams**:
- Admin builds a bank of 100+ MCQs (text, 4 options, correct answer) and a
  bank of practical questions tagged easy/hard.
- Admin creates an exam: pick a section, pick exactly 1 easy + 1 hard
  practical from the bank, set a start/end window, plus a manual open/close
  override (both apply — scheduled window AND admin can force it early).
- Each student gets 5 MCQs picked at random from the whole bank the first
  time they open the exam; that set is then fixed for them and persists
  across reload (resumable, one attempt).
- Student answers only the 5 MCQs (radio choice). The 2 practical questions
  are shown as read-only text — no answer box, no submission for those.
- On submit: student immediately sees their MCQ score (e.g. "3/5").
- Admin sees a results page per exam: every student's score + section average.

**Phases:**
1. Tasks — backend (model, API, tests).
2. Tasks — frontend (admin create/manage page, student view page, nav).
3. Exam question bank — backend (MCQ + practical models, CRUD API, tests).
4. Exam question bank — frontend (admin page to add/edit/tag questions).
5. Exam scheduling — backend (Exam model: section, practicals, window,
   manual toggle; list/create/update API, tests).
6. Exam attempts — backend (assign-5-random-MCQs-once, resumable, submit +
   score, one-attempt guard; API, tests).
7. Exam — frontend admin (builder page, results page).
8. Exam — frontend student (take-exam page: MCQs + read-only practicals,
   submit, see score; nav/dashboard tiles for both features).
9. Deploy: rebuild zips, update DEPLOY.md if anything server-side changed.

## Work Log

Every phase/work update gets an entry here, newest first.

- **2026-09-24** — Course Syllabus / TOC page. The user's "TOC page" turned out to mean a 48-session AI Training curriculum from an uploaded `toc.pdf` (Session # + Key Topics), not document.md chapters — clarified via question, then built as a real in-app page rather than static markdown. New `syllabus` app: `SyllabusSession` model (`session_number` unique, `topics`), seeded with all 48 rows from the PDF via a data migration (`0002_seed_curriculum`, `get_or_create` so it's safe to re-run). `GET /api/syllabus/` — any signed-in user (student or teacher) reads the full list. `/api/syllabus/manage/` + `/manage/<id>/` — teacher-only add/edit/delete. `frontend/src/pages/SyllabusPage.tsx` — one shared component at `/syllabus` for both roles; a teacher sees Add/Edit/Delete controls, a student sees the same table read-only. Linked from the teacher's "Tasks & Exams" nav dropdown, a student nav link, and a dashboard tile. Verified: 6 new tests pass, full suite 291/291, `npm run build` passes. Deploy zips rebuilt (144 backend files).

- **2026-09-24** — Today's Timetable card on the teacher dashboard now shows a "N present" green stamp next to any slot that already has a session (started or closed) — `TodayScheduleView` adds `present_count` (an `Attendance.objects.filter(session=session).count()`, `null` when no session exists for that slot yet) to each schedule block. Also added a Table of Contents section to `document.md` (unrelated, same day, separate user request) linking all 34 existing chapters, with a note to append new chapter entries there. Verified: 2 new schedule tests pass, full suite 285/285, `npm run build` passes. Deploy zips rebuilt.

- **2026-09-24** — Teacher navbar was 11 buttons wide (screenshot: unreadable wall of pills) after the Tasks/Exam System phases. Grouped into 3 dropdowns per the user's exact grouping: **Attendance** (Analytics, Student Data, Timetable), **Tasks & Exams** (Tasks, Question Bank, Exams), **Records** (Profile Corrections, Password Reset OTP History). Profile, Day-wise and Log Out stay as standalone buttons (not asked to be grouped). `AppShell.tsx` now controls the mobile-collapse `expanded` state itself, since a plain `Dropdown` (not `NavDropdown`) doesn't participate in `Navbar`'s built-in `collapseOnSelect` — every link and dropdown item now closes the mobile menu via an explicit `onClick`. Student navbar unchanged (only 5 items, not cluttered). Verified: `npm run build` passes, `manage.py check` clean (no backend change). Deploy zips rebuilt.

- **2026-09-24** — Bulk CSV upload for MCQs, added after the 9-phase plan finished (user follow-up request). `POST /api/exams/mcq/bulk-upload/` (teacher-only, multipart) — CSV columns `text,option_a,option_b,option_c,option_d,correct_option` (any order, case-insensitive header). Each row validated independently: good rows are `bulk_create`d, bad rows (missing field, invalid `correct_option`) are reported by row number without blocking the rest of the file; blank lines are skipped silently. `QuestionBankPage`'s MCQ tab got an "Upload CSV" button next to "+ Add MCQ", showing created-count and any per-row errors in a scrollable list. Verified: 7 new tests pass, full suite 283/283, `npm run build` passes. Deploy zips rebuilt (132 backend files).

- **2026-09-24** — Phase 9 (final) of the Tasks + Exam System plan done: deploy prep. Rebuilt `frontend/dist.zip` and `deploy/classpulse-backend.zip` (131 files, includes the new `tasks` and `exams` apps, no `.env`/media/db leaked — checked). Not uploaded to cPanel — that's the user's step. To deploy: upload both zips, run `scripts/post_deploy.py` (migrates the two new apps' tables), restart the app, hard-refresh the frontend. No new one-off script needed for this batch (no data migration, just new tables). **All 9 phases of the Tasks + Exam System plan are now done.**

- **2026-09-24** — Phase 8 (final phase) of the Tasks + Exam System plan done: Exam frontend student. `frontend/src/pages/student/ExamsPage.tsx` — lists exams for the student's section with status (not_started/in_progress/submitted) and score once submitted; `frontend/src/pages/student/ExamTakePage.tsx` — the take-exam screen: 5 MCQs as radio groups, each answer auto-saved to the server on selection (optimistic UI, reconciled against the server response); the 1 easy + 1 hard practical shown as plain read-only text with no answer box, exactly per spec; "Submit Exam" confirms first (warns if not all 5 are answered), then shows the MCQ score immediately and locks further changes; if the window closes before submitting, answering is disabled but the student can still submit what they already answered. New routes `/student/exams` and `/student/exams/:examId`, nav link. **Both features (Tasks and Exam System) are now fully built end-to-end, backend and frontend, all 8 phases done.** Verified: `npm run build` passes, full backend suite 276/276 (unaffected — no backend change this phase). Not yet deployed — needs the backend zip (two new apps: `tasks`, `exams`) and frontend build uploaded; not checked in a real browser.

- **2026-09-24** — Phase 7 of the Tasks + Exam System plan done: Exam frontend admin. `frontend/src/pages/teacher/ExamsPage.tsx` — section tabs, schedule/edit modal (title, section, easy/hard practical dropdowns filtered to active questions of that difficulty, start/end datetime pickers, manual override select), Force open/Force close/Follow schedule quick actions per exam, delete. `frontend/src/pages/teacher/ExamResultsPage.tsx` — attempted/submitted/class-average tiles + per-student score table, linked from each exam card. New routes `/teacher/exams` and `/teacher/exams/:examId/results`, nav link, dashboard tile. Verified: `npm run build` passes, `manage.py check` clean (no backend change this phase). Remaining: Phase 8, the student-facing take-exam page — the whole feature is unusable by students until that lands.

- **2026-09-24** — Phase 6 of the Tasks + Exam System plan done: Exam attempts backend (`exams/attempts.py`). Student: `GET /api/exams/student/exams/` (list exams for their own section with their attempt status: not_started/in_progress/submitted + score); `GET /api/exams/student/exams/<id>/` (creates the attempt on first call — 5 MCQs picked at random from currently-active questions, fixed from then on; resuming returns the same 5, never re-randomized; 403 if the exam isn't open yet and no attempt exists, or if the exam is for a different section); `PATCH` same URL to save answers (rejects an unknown question id, an invalid option letter, answering after submit, or answering after the window closes); `POST .../submit/` scores against `correct_option` and locks the attempt — idempotent, a retry after a successful submit just returns the same score instead of erroring. Students are served `StudentMCQSerializer` (no `correct_option` field ever sent to them). Teacher: `GET /api/exams/exam/<id>/results/` — every student's score/status + class average. Verified: 32 new tests pass (attempts + results), full suite 276/276, `manage.py check` clean. No frontend yet — Phases 7-8.

- **2026-09-24** — Phase 5 of the Tasks + Exam System plan done: Exam scheduling backend. `Exam` model (section, one `easy_practical` + one `hard_practical` FK — validated to actually be tagged that difficulty, `start_time`/`end_time` window, `manual_status`: auto/forced_open/forced_closed per the user's "both" answer) and `ExamAttempt` model (added now, API comes Phase 6: `mcq_ids` fixed-at-first-open list, `answers` dict, `score`, one-attempt-per-student-per-exam DB constraint). `is_open()` follows the window unless manually forced either way. Teacher-only CRUD: `/api/exams/exam/`, `/api/exams/exam/<id>/`, `?section=` filter. Creating an exam requires at least 5 active MCQs to already exist in the bank. Verified: 19 new tests pass, full suite 263/263, `manage.py check` clean. No frontend yet, no attempt-taking API yet — Phases 6-8.

- **2026-09-24** — Phase 4 of the Tasks + Exam System plan done: Exam question bank frontend. `frontend/src/pages/teacher/QuestionBankPage.tsx` — MCQ / Practical tab toggle, add/edit modal (MCQ modal has a radio per option to mark the correct one), Retire/Reactivate (toggles `is_active` without deleting), Delete with confirm. New route `/teacher/question-bank`, nav link, dashboard tile. Verified: `npm run build` passes, `manage.py check` clean (no backend change this phase).

- **2026-09-24** — Phase 3 of the Tasks + Exam System plan done: Exam question bank backend. New `exams` app: `MCQQuestion` (text, 4 options, `correct_option`, `is_active`) and `PracticalQuestion` (text, `difficulty` easy/hard, `is_active`) models. Teacher-only CRUD: `/api/exams/mcq/`, `/api/exams/mcq/<id>/`, `/api/exams/practical/`, `/api/exams/practical/<id>/`, both listable filtered by `?active=` (practical also `?difficulty=`). `is_active=False` retires a question from future exams without touching any exam that already drew it. Verified: 8 new tests pass, full suite 252/252, `manage.py check` clean. No frontend or Exam/attempt model yet — those are Phases 4-6.

- **2026-09-24** — Phase 2 of the Tasks + Exam System plan done: Tasks frontend. `frontend/src/pages/teacher/TasksPage.tsx` — section tabs (same pattern as Timetable), add/edit modal, delete with confirm. `frontend/src/pages/student/TasksPage.tsx` — read-only list for the student's own section, overdue due-dates shown in red. New routes `/teacher/tasks` and `/student/tasks`, nav links in `AppShell`, a "Tasks" tile on the teacher dashboard. Verified: `npm run build` passes, `manage.py check` clean (no backend change this phase).

- **2026-09-24** — Phase 1 of the Tasks + Exam System plan (see above) done: Tasks backend. New `tasks` app: `Task` model (title, description, section, due_date, created_by), `GET/POST /api/tasks/teacher/` + `GET/PATCH/DELETE /api/tasks/teacher/<id>/` (teacher-role only, optional `?section=` filter), `GET /api/tasks/student/` (student-role only, auto-scoped to their own `StudentProfile.section`, 403 with a clear message if their profile has no section). No submission model — view-only per spec. Verified: 8 new tests pass, full suite 244/244 pass, `manage.py check` clean. No frontend yet — that's Phase 2.

- **2026-09-24** — Added Non-Negotiable Rule 8 (student photo/profile data safety) after `scripts/find_missing_photos.py --fix` correctly cleared 193 students' broken `photo` references on 2026-09-23. The fix itself was right and asked-for, but the user asked for a standing rule so this class of action — a script that bulk-writes to every student's data — never happens again without them explicitly asking that day, and never gets folded into a routine/automatic deploy step. See Rule 8.

- **2026-09-23** — Broken profile-photo images (reported: photos students uploaded before the latest production update show as a broken image; new uploads are fine). Root cause is on the server, not in this repo's code: the DB still points at old photo filenames, but the files themselves are gone from `MEDIA_ROOT` on disk — most likely wiped or never carried over during a prior manual deploy/reorg (backend's `media/` folder is excluded from every deploy zip on purpose, so a repo deploy can't have caused this, but a File Manager cleanup or a docroot overlap with the frontend `dist/` extraction could have). Two fixes shipped: (1) new `Avatar` component (`frontend/src/components/Avatar.tsx`) used in `LiveQRPage`, `StudentDataPage`, and `StudentProfilePage` — falls back to the student's initial on an `onError` from the `<img>`, so a dead photo URL never again shows the browser's broken-image icon; (2) `scripts/find_missing_photos.py` — checks every `StudentProfile.photo` against disk, lists whoever's file is missing, and with `--fix` clears those DB references so the profile correctly shows "Add photo" instead of continuing to point at nothing. Run it read-only first to see who's affected before deciding whether to `--fix`; those students need to re-upload since the original files are gone. Verified: `npm run build` passes, `python manage.py check` clean; did not re-run the full test suite for this change (UI-fallback + a new read/write script, no logic in existing tested paths touched). Deploy zips rebuilt.

- **2026-09-22** — Big upgrade batch (backend + frontend), deploy zips built. **Profile corrections:** in-app `/teacher/corrections` review page (approve/reject) replaces the dead "Review in Admin" link; approve/reject logic lives in `accounts/services.py`, shared with the Django admin actions; approval rejects a CRN already owned by another student and keeps `username` (and the default password, only if still default) in sync with the CRN. **Scan block:** `IncompleteProfileError` now names exactly what is missing ("Add your email and contact number to mark attendance.") and the mark API returns `code: "incomplete_profile"` + `missing_fields`; the scan page popup has a "Complete profile" button that opens the student profile, where missing photo/email/contact are red-marked and clear live (profile API returns `missing_scan_fields` + `scan_lock_enabled`). Student profile redesigned (Scan Attendance hero first). **Placeholder emails:** migration `accounts.0010_clear_placeholder_emails` blanks every `@bba.local` email; `import_students.py` no longer creates them. **Login:** username matches case-insensitively and, only while the password is still the default CRN, ignores case/whitespace in the password (phone keyboards were causing "invalid" logins). **Logout lock:** students cannot log out for 10 minutes after sign-in (`LogoutView` 403 `logout_locked`, judged by `Token.created`; Log Out button waits on the server) to stop hopping into a friend's account — a deterrent only, clearing browser storage still bypasses it; a device-bound sign-in lock would need a new model. **Late report:** `GET /api/attendance/late-report/` (`attendance/late_report.py`) — minutes from session start to each student's own scan (manual attendance excluded), thresholds 0/5/10/15/30, per-student times-late / late-rate / avg / worst, "regular" flag (3+ late and over half of scans), per-section summary; shown as the Late Arrivals panel on Analytics with sortable columns and CSV export. Lateness counts from when the teacher started the session, and a QR window shorter than the threshold makes that lateness impossible (use 15+ minute windows). **Timetable editor:** `/api/attendance/timetable/` CRUD (`attendance/timetable.py`, teacher role — the app has no separate admin role) with end>start and per-section overlap validation; weekly-grid `/teacher/timetable` page. **Attendance history** now newest first. **UI:** 3D spinning-QR-cube loading screen, shared `PageHeader`/stat tiles/table styling, redesigned teacher dashboard, Start Attendance, attendance history, change password and scan pages, content column capped at 1450px with fluid gutters. Verified: 236 accounts+attendance tests pass, `npm run build` passes. Not verified in a browser (headless screenshots were unusable). Mistake to avoid: a `taskkill /IM chrome.exe` cleanup closed the user's own Chrome — kill only the spawned PID. Deploy: upload `deploy/classpulse-backend.zip` (extract over `/home/fmmkirqf/classpulse-backend`, never overwrite `.env`) and `frontend/dist.zip` (extract `dist/` contents into the site root), run `scripts/post_deploy.py` (runs migration 0010) and `scripts/fix_username_crn_mismatch.py`, then restart the Python app.

- **2026-09-17** — ERP attendance prompt generator. Added a `Create Prompt` button to the teacher Day-wise Attendance page beside the CSV export. It uses the currently selected section/date and all students marked present to generate an ERP-ready instruction prompt matching the requested structure: section/date, instructions to mark listed students Present and leave others Absent, a `Present students (CRN — Name)` list, total present count, and a pre-submit confirmation instruction. The prompt is copied to clipboard when browser permissions allow and is also shown in a modal preview with a manual Copy button. Verified: `npm run build` and `npm run lint` pass.

- **2026-09-17** — Admin-controlled profile-completion scan lock. Added a global `StudentScanPolicy` model (`accounts.0009_studentscanpolicy`) and a Student Data page button that lets the teacher/admin enable or disable requiring complete student profiles before QR attendance scans. When enabled, `mark_attendance()` blocks students missing profile photo, non-placeholder email, or contact number with a clear "Complete your profile" error; when disabled, scans behave as before. Student Data now shows the policy state, counts incomplete visible students, marks each row Complete/Incomplete, and shows missing fields on the selected student detail. Added Django admin visibility for the policy. Verified: `python manage.py check`, `python manage.py makemigrations --check --dry-run`, `python manage.py test accounts.tests.test_teacher_student_data attendance.tests.test_mark_attendance`, `npm run build`, and `npm run lint` pass.

- **2026-09-16** — cPanel Python app path fix. The cPanel screenshots showed `Incorrect server response`, `Run script: Unknown error occurred`, and `APPLICATION DOES NOT EXIST` while the Python app row still showed `started`. The repo deploy script was still using placeholder paths (`/home/USERNAME/...`) and Python 3.10, but the actual cPanel app is `/home/fmmkirqf/classpulse-backend` on Python 3.11. Updated `.cpanel.yml` to use `/home/fmmkirqf/classpulse-backend` and `/home/fmmkirqf/virtualenv/classpulse-backend/3.11/bin/{pip,python}`. Added `DEPLOY.md` troubleshooting steps for cPanel's Python App UI errors and the separate Chrome `ERR_QUIC_PROTOCOL_ERROR` symptom.

- **2026-09-16** — Local database migration catch-up after loading stall. The app showed long loading on Analytics/Student Data because the local MySQL schema was behind the code: `accounts_studentprofile.trusted_device_hash` did not exist yet, so protected API requests errored while the frontend stayed on its loading screen. Ran `python manage.py migrate`, applying `accounts.0007_studentprofile_trusted_device`, `accounts.0008_studentprofile_section_crn_index`, and `attendance.0014_analytics_indexes`. After migration, local timings were healthy: Analytics first load ~65ms, cached Analytics ~3ms, Student Data ~9ms. Also removed the Student Data frontend double-fetch so the page no longer loads all 390 students before settling on the first section. Verified: `python manage.py check`, `python manage.py test accounts.tests.test_teacher_student_data attendance.tests.test_analytics`, `npm run build`, and `npm run lint` pass.

- **2026-09-16** — Faster Analytics page loading. Replaced the Analytics endpoint's full student-by-session matrix calculation with a database aggregate summary (`SUM(session.periods) GROUP BY student_id`) so the page no longer loops across every student and every closed session just to show totals/percentages. Export code still uses the full matrix because CSV/Excel/PDF need per-session columns. Added DB indexes for analytics filters/lookups: `AttendanceSession(status, date)`, `AttendanceSession(section, date)`, `Attendance(session, student)`, and `StudentProfile(section, crn)`. Verified: `python manage.py check`, `python manage.py makemigrations --check --dry-run`, and `python manage.py test attendance.tests.test_analytics attendance.tests.test_attendance_matrix accounts.tests.test_teacher_student_data` pass. A broader export test run reached PDF tests but local Python is missing the optional `pypdf` package used by that assertion.

- **2026-09-15** — Device binding changed back to warning-only. Removed the temporary one-student-one-phone login blocking rule: student/admin login no longer binds or rejects based on `X-ClassPulse-Device-ID`. Attendance scans still remember the first scan device for each student, but a later scan from a different device, or the same phone being used by another student, now logs `new_device` suspicious activity for teacher/admin visibility without rejecting the attendance mark. Student Data/Django admin trusted-device status and reset actions remain available for investigation and cleanup. Updated regression tests to cover login not blocking, changed-device scans being allowed and logged, and same-phone second-student scans being allowed and logged.

- **2026-09-15** — One device, one student login enforcement. Strengthened the trusted-device feature from scan-only to login-level binding: the frontend keeps a persistent `classpulse_device_id` in localStorage (not cleared on logout) and sends it on login; `RoleAwareLoginView` now binds the first student who logs in on that phone/browser and rejects attempts to log in as a different student from the same device, or to log in as the same student from a second device, until admin resets the trusted device. Extracted shared device logic into `accounts/device_security.py` so login and attendance scans use the same hash/bind/reject rules. Attendance marking still logs rejected second-device scans as `new_device`, and Student Data/Django admin reset actions remain the recovery path for legitimate phone changes or cleared site data. Teachers are not device-bound by this rule. Verified: `python manage.py check`, `python manage.py test accounts.tests.test_login attendance.tests.test_new_device attendance.tests.test_mark_attendance accounts.tests.test_teacher_student_data`, `npm run build`, and `npm run lint` pass.

- **2026-09-15** — Trusted-device binding for attendance scans. Added a scan-only device security layer to reduce friend-login/friend-scan abuse: the frontend now creates a stable per-browser random `classpulse_device_id` and sends it as `X-ClassPulse-Device-ID`; the backend hashes that value and stores it on `StudentProfile` as the student's trusted scan device on the first successful scan. Later attendance scans for the same student from a different device id are rejected with a clear message and logged as `new_device` suspicious activity instead of being allowed. If a student changes phone/browser or clears site data, teacher/admin can reset the trusted device from the new Student Data page (`reset_trusted_device`) or Django admin (`Reset selected trusted devices`), after which the next successful scan binds the new phone. Login/profile viewing remains allowed from other devices; only QR attendance marking is blocked. Added migration `accounts/0007_studentprofile_trusted_device.py`, updated Student Data API/UI to show linked/not-linked device status and reset action, and removed the old behavior where new devices were merely logged after successful attendance. Verified: `python manage.py check`, `python manage.py test attendance.tests.test_new_device attendance.tests.test_mark_attendance accounts.tests.test_teacher_student_data`, `npm run build`, and `npm run lint` pass.

- **2026-09-15** — Modern purple UI refresh and login back-link. Retuned the global design tokens in `frontend/src/index.css` from the older paper/register look toward a modern purple app theme: brighter violet primary, cyan accent, glassy cards, softer shadows, animated page/card entry, smoother button/timetable hover states, improved form/table surfaces, and reduced/removed negative letter-spacing in shared styles. Kept existing Bootstrap/react-bootstrap structure and app workflows intact. Added a `Back to AI World` button on the login card linking to `https://aman-0402.github.io/AI-World/`. Verified: `npm run build` and `npm run lint` pass.

- **2026-09-15** — Teacher/admin Student Data page. Added `GET/POST /api/teacher/students/` (`TeacherStudentDataView`) for teacher-only section-based student lookup: returns available sections, filtered student summaries, and a selected student's full profile/photo/contact details plus attendance history (date, subject, section, marked time, IP, device). Added `POST` action `reset_password_to_crn` so admins can reset a forgotten password safely without exposing any existing password. Explicitly does **not** reveal the current password — Django stores only a one-way password hash, and showing passwords would be both impossible and unsafe; the UI/API instead show a password note plus reset action. Added `StudentDataPage` under `/teacher/students`, linked from the teacher navbar as "Student Data", with section filter, search, clickable student rows, detail panel, photo/initial fallback, attendance history list, and reset-password button. Improved Django admin student inspection at the same time: clickable name/CRN, profile/photo fields, inline attendance-history table, Attendance admin filters/search, and reset-selected-passwords-to-CRN action. Verified: `python manage.py check`, `python manage.py test accounts.tests.test_teacher_student_data`, `npm run build`, and `npm run lint` pass.

- **2026-09-15** — External QR scanner support, slower-scan grace window, student install button, and SEO/PWA metadata. Changed the teacher live QR from encoding the raw opaque token to encoding the frontend scan URL (`https://arxinfo.info/student/scan?token=...`) so Google Lens, phone camera apps, and generic QR scanners open ClassPulse instead of showing a random token. Updated the protected-route/login flow to preserve the originally requested student scan URL through login, then return the student there after successful authentication. Updated `ScanQRPage` to accept `?token=` directly and still support the old raw-token QR format when scanned inside the in-app camera scanner. Fixed a real follow-up issue where normal external scanning could fail because the same 15s value controlled both visible QR rotation and token validity — now QR display still rotates every 15s, but token acceptance lasts 60s so Google Lens/browser/login delay does not immediately turn a valid scan into "try again"; backend validation remains the source of truth and still rejects closed sessions, wrong section, duplicates, invalid tokens, and expired tokens. Added a regression test proving an old token can remain valid while the displayed QR rotates to a new token. Added a persistent `Install App` button on the student profile, using the browser's native PWA install prompt where available and iOS Add-to-Home-Screen guidance otherwise. Added/updated PWA and SEO files: manifest description/scope/categories, index meta description/keywords/canonical/Open Graph/Twitter/JSON-LD, `robots.txt`, and `sitemap.xml`. Follow-up refinement: `ScanQRPage` now shows a camera-permission explainer before starting the camera, direct `?token=` scans now render a clear success/already-marked/error panel instead of relying only on toast messages, and `frontend/public/.htaccess` handles React deep links plus redirects `www.arxinfo.info` to `arxinfo.info` while leaving `/api` to Django. Also commented out `DB_SOCKET` in `backend/.env.example` and added `www.arxinfo.info` to the example `ALLOWED_HOSTS`/`CORS_ALLOWED_ORIGINS` so copying the example no longer forces a host-specific MySQL socket path and both domains are deploy-ready. Verified: `python manage.py test attendance.tests.test_qr attendance.tests.test_mark_attendance` passes (16/16), `npm run build` passes, `npm run lint` passes.

- **2026-08-14** — Teacher self-service email update, plus Name/Subject rows on the teacher profile card. Continuation of the student-side editable-email feature: the user flagged `TeacherProfilePage`'s "Email: admin@classpulse.local" row as read-only and asked for it to be editable, then mid-turn asked for the card to also show the teacher's name and subject (the student card already shows Roll No./Course/Semester/Section/Email, teacher's had none of the equivalent identity rows). Generalized the existing `UpdateEmailView` (previously student-only, `IsStudent`-gated) to be role-agnostic (`IsAuthenticated` only), reused via two URLs — `/api/student/email/` and the new `/api/teacher/email/` — pointing at the same view, branching only the response serializer (`StudentProfileSerializer` vs `TeacherProfileSerializer`) by `request.user.role`. Removed the now-incorrect `test_teacher_cannot_use_student_endpoint` test (its 403 assumption no longer holds under the loosened permission) and added a full `TeacherUpdateEmailTest` class. Added a `Name` row (`profile.full_name || profile.username`, same fallback already used in the welcome heading) and a `Subject` row using a new shared `TRAINING_SUBJECT` constant (`frontend/src/constants.ts`) — extracted specifically so the literal `"AI Training"` isn't duplicated independently in both `StartAttendancePage.tsx` and `TeacherProfilePage.tsx`, which could otherwise drift apart later. Email row reuses the same click-to-edit inline pattern (Edit → input + Save/Cancel) as the student page. 3 new backend tests (teacher can update own email, invalid email rejected, unauthenticated rejected). Full suite: 148/148 passing, `tsc --noEmit` clean. Verified live: logged in as the real `admin` teacher account, screenshotted the card showing Name/Subject/Email rows, clicked Edit, changed the email, saved, confirmed it persisted — then reverted it back to `admin@classpulse.local` via the same flow so the real account wasn't left mutated, confirmed via a DB read afterward.

- **2026-08-14** — Self-service email update on the student profile. All 415 students were imported with a placeholder `{urn}@bba.local` email (never a real address), and the profile page showed it read-only. Added `POST /api/student/email/` (`UpdateEmailView`) — deliberately direct self-service, not routed through `ProfileEditRequest`/admin-approval like name/CRN/roll-number are, since email doesn't affect QR-scan attribution or attendance-fraud risk the way those identity fields do. `StudentProfilePage`'s Email row is now click-to-edit inline (Edit → input + Save/Cancel), no page navigation. 4 new backend tests. Verified live: edited a test student's placeholder email to a real address, saved, confirmed it persisted and edit mode closed correctly. Full suite: 146/146 passing.

- **2026-08-14** — Resume attendance after auto-close, and manual present/absent toggle directly on the live roster (not just the separate Day-wise page). Real dead-end reported: 5-min window auto-expires, teacher clicks the timetable slot again, lands back on the same session's `LiveQRPage` — which just showed a permanently broken QR spinner ("QR refreshes in --s") with no way forward, since `SessionQRView` correctly 400s for a closed session and nothing in the UI offered a next step. Added `POST /api/attendance/sessions/<id>/resume/` (`ResumeSessionView`) — reactivates a closed session by resetting `start_time` to now (same `duration_minutes`, fresh window), explicitly preserving all `Attendance` rows already recorded rather than starting a new session (verified with a dedicated test). `LiveQRPage` now shows a "Resume Attendance" button in place of the dead QR area whenever the window's closed, and immediately re-fetches the QR + live data on success rather than waiting for the next poll tick. Also wired the roster's status column to `ManualAttendanceView` (which already existed for Day-wise) — clicking a name's Present/Absent stamp toggles it right there, with an optimistic local update plus a present-count adjustment, no page navigation needed. 8 new backend tests (resume reactivates/rejects-if-already-active/rejects-other-teacher's-session/preserves-attendance, and the exact reported scenario — an auto-expired session's window is genuinely fresh after resume, not still expired against the old `start_time`). Verified both live end-to-end: created a session pre-expired exactly like the report, resumed it (QR came back, "04:58 LEFT"), then clicked a roster row's status directly (0/60 → 1/60, Absent → Present). Full suite: 142/142 passing.

- **2026-08-14** — Fixed timetable card layout regression on `TeacherProfilePage`: time range, subject, section badge, and action badge were wrapping mid-cell ("10:05 AM –" / "11:55 AM" on separate lines) — the card was still capped at the old `maxWidth: 620` from before the font-size bump, too narrow for four columns at 18px base font. Widened to `820px` and added `text-nowrap` to every cell so none of them wrap regardless of width pressure. Verified live in-browser: all four columns now render on one line.

- **2026-08-14** — Removed Django Channels entirely and added CI/CD + a shared-cPanel deploy path, per the user's explicit choice after I laid out that shared cPanel (Passenger, WSGI-only, no root, no Redis) fundamentally cannot run this app's WebSocket architecture — not a config issue, a hard mismatch. Chose "strip real-time features, deploy WSGI-only" over the VPS alternative.
  - **Backend**: deleted `attendance/consumers.py`, `attendance/routing.py`, `attendance/ws_auth.py`, `classpulse/asgi.py`, and every WS-only test (`test_consumer.py`, `test_ws_auth.py`, `test_broadcast.py`, `test_activity_broadcast.py`). Removed `broadcast_attendance_update()`/`broadcast_activity_event()` from `services.py` — `ActivityLog` writes and `Attendance` creation are unaffected, only the WS push is gone. Removed `daphne`/`channels`/`channels_redis` from `INSTALLED_APPS`, `CHANNEL_LAYERS`, `ASGI_APPLICATION`, `REDIS_URL`, and the now-dead `BACKEND_ORIGIN` setting. Rebuilt `requirements.txt` from a trimmed list validated against a **fresh venv** (not guessed) — 9 direct packages, 15 total incl. transitive deps, down from 25+. `manage.py runserver` now genuinely serves plain WSGI (`Server: WSGIServer`, confirmed via `curl`, not `daphne`) — exactly what cPanel's Passenger needs.
  - **Frontend**: deleted `api/ws.ts`. `LiveQRPage` now polls `getSessionLive()`/`getSessionActivity()` every 3s instead of holding a WebSocket open; a `Set`-based "already toasted this scan/activity" ref prevents re-announcing unchanged data on each poll, and the very first poll never toasts (it's history, not something that just happened). Present count, roster, and the recent-scan photo panel all still update automatically — same UX, different transport.
  - **Verified for real**: drove a manual-attendance mark from outside the browser (pure REST, no WS anywhere in the stack) while a teacher's `LiveQRPage` sat open — the toast, present count (0→1), and roster row all updated within one 3s poll cycle, screenshotted before/after.
  - **CI**: added `.github/workflows/ci.yml` — backend job runs the full Django suite against a real `mysql:8.0` service container (not sqlite, matching prod), frontend job runs `tsc -b && vite build`. Verified the frontend build succeeds locally first (`npm run build`, 982ms, warns about one >500kB chunk but is not a hard failure).
  - **CD**: added `.cpanel.yml` (cPanel Git Version Control's native deploy-script format — copies `backend/`, `pip install`, `migrate`, `collectstatic`, touches `tmp/restart.txt` to reload Passenger) and a `deploy-frontend` CI job that FTP-uploads the built `dist/` to cPanel, gated behind a `CPANEL_DEPLOY_ENABLED` repo variable so it's a safe no-op until the user actually configures FTP secrets for their real host. Wrote `DEPLOY.md` documenting the full setup (Setup Python App, env vars, Git Version Control, FTP secrets) and explicitly what stays manual (cPanel's UI deploy click — most shared hosts don't expose the SSH/API access a fully automatic trigger would need).
  - Full suite: 137/137 passing (down from 153 — the 16 removed were exactly the WS/broadcast tests for code that no longer exists, not a coverage loss).

- **2026-08-14** — Three related features: student profile-correction requests, profile photos, and live photo on scan.
  - **Profile edit requests**: students can't edit name/CRN/roll number directly (identity integrity), but can now request a change via a new `ProfileEditRequest` model — one pending request at a time (blocks resubmission until reviewed), any subset of name/CRN/roll number plus an optional reason. Approval happens through Django admin (`ProfileEditRequestAdmin`), consistent with how this app already handles all other admin-only management (`ClassSchedule`, `StudentProfile`) — an "Approve" action actually applies the change to the `User`/`StudentProfile` fields that were requested (only those, others untouched) and stamps `reviewed_by`/`reviewed_at`; "Reject" just marks it. `StudentProfilePage` shows a pending-request banner (with what was requested) instead of the form while one's outstanding.
  - **Profile photos**: `StudentProfile.photo` existed but was write-only-via-registration, and registration is gone — added `POST /api/student/photo/`, validated server-side (not just client-side) for ≤1MB and exactly square dimensions via Pillow, since client-side checks are trivially bypassed. Discovered and fixed a real gap while building this: `MEDIA_URL` was never wired to `urlpatterns` in dev, so uploaded photos would have 404'd on every `<img>` tag — added the `static()` fallback for `DEBUG=True`.
  - **Live photo on scan**: the teacher's "who just scanned" panel (previously name+CRN only) now shows a circular photo thumbnail (initial-letter avatar fallback when no photo set), delivered the same way present-count already was — no new polling. `broadcast_attendance_update()`'s WS payload gained `photo`; since that code path has no `HttpRequest` to build an absolute URL from, added a `BACKEND_ORIGIN` setting (env-configurable, defaults to `http://127.0.0.1:8000`) for that one case — `SessionLiveView`'s REST fetch already had a request to build the URL from properly.
  - Verified the full loop live end-to-end, not simulated: a real student uploaded a photo and submitted an edit request (pending banner appeared correctly), a teacher started a real Section D session, the student's `mark_attendance` call fired from outside the teacher's browser session, and the photo thumbnail appeared live in the teacher's recent-scans panel within ~1.5s — placed exactly where the user asked, directly below where "Waiting for students to scan..." had been.
  - 18 new backend tests (edit-request create/list/pending-block/re-request-after-review/isolation-between-students/role-gating, admin approve applying only requested fields, photo upload square/oversize/non-square/role-gating, broadcast payload carrying an absolute photo URL). Full suite: 153/153 passing.

- **2026-08-14** — Client-side pagination (70/page, per the user's explicit number) on the two student tables that could grow large: Analytics (390 students today, no upper bound) and Day-wise (per-section, currently maxes at 70 but not guaranteed to stay there). Added a small reusable `TablePagination` component (Previous/Next + "Page X of Y", hides itself entirely when everything fits on one page) rather than duplicating pager logic per page. Page resets to 1 whenever a filter changes (section/date-range/search on Analytics, section/date on Day-wise) so switching filters never leaves you stranded on an empty page. S.No numbering stays globally correct across pages (page 2 continues 71, 72... not restarting at 1). Verified live: Analytics with all 390 students correctly shows "Page 1 of 6", Next advances to rows 71-140; Day-wise's largest section (70 students) correctly shows all of them with no pager (since 70 ≤ page size, nothing to paginate yet — pager will engage automatically once a section grows past 70).
- **2026-08-14** — UI typography upgrade: bumped the root font from 16px/1.5 to 18px/1.6 in `index.css`. Since every Bootstrap 5 CSS variable in this design system is already rem-based (`--bs-border-radius`, table padding, button sizing, heading scale, etc.), this one change cascades the size increase through the entire app — no per-component overrides needed. Also widened `.app-shell-main` (1080px → 1180px) and gave table cells more breathing room (`.table > :not(caption) > * > *` padding 0.5rem → 0.75rem/0.9rem) to match. Caught and fixed a real regression from the font bump before shipping it: the `.stamp` badge (the signature pill UI used for section tags, Present/Absent, Start/View Attendance buttons) started wrapping mid-text inside narrow timetable cells — "BBA III D" broke onto two lines. Added `white-space: nowrap` to `.stamp`, verified via screenshot that it now stays on one line everywhere. Kept the existing chalkboard/paper/brass identity and component structure untouched — this was a typography/spacing pass, not a re-brand, since a full visual redesign had already been done earlier in this project.

- **2026-08-14** — Fixed the last flagged item from the security audit: `CHANNEL_LAYERS` was in-memory-only, meaning real-time WebSocket broadcasts (present-count, live roster, suspicious-activity toasts) would silently stop reaching other server processes the moment this ran as more than one daphne worker — a real blocker for any actual deployment, not just a theoretical one. Installed Memurai Developer (a genuine Redis-protocol-compatible Windows service, free dev tier) since no Redis/Redis-alternative was present on this machine, and `channels_redis`. `CHANNEL_LAYERS` is now driven by `REDIS_URL`: set → `channels_redis.core.RedisChannelLayer`; unset → falls back to the old in-memory layer, so tests and a bare `runserver` still work with nothing extra installed. Added `REDIS_URL=redis://127.0.0.1:6379` to `backend/.env` and documented it (commented, optional) in `.env.example`. Verified for real, not just via settings inspection: full 140/140 suite passed with Redis active, then drove an actual end-to-end scan through the running app — teacher's `LiveQRPage` open in a browser with its WebSocket connected, a real student's `mark_attendance` API call fired from outside that browser session, and the toast ("Pragya Savita marked present"), present count (0→1), and roster row (Absent→Present) all arrived live — proving the broadcast is genuinely flowing through Redis pub/sub now, not the in-memory layer. (Also applied the lesson from the last cleanup mistake: deleted the test session by exact `id`, not by a field real sessions share.)

- **2026-08-14** — Security/audit pass: after finding two accidental data-loss incidents from my own test cleanup (deleted a real in-progress session by matching on the shared "AI Training" subject text, and a 5-minute test session auto-expiring before the user could open it), did a real audit of the app rather than more ad-hoc fixes — checked `settings.py`, permissions, throttling config, password scheme, and token lifecycle directly against the code. Found and fixed the highest-priority items:
  - **Predictable student passwords**: the scheme (first 4 letters of name + CRN suffix) is derivable from public info, which undermines the wrong-section QR lock entirely — an attacker just logs in as the right student. Added self-service password change: `POST /api/change-password/` (validates old password, runs Django's password validators on the new one, rotates the auth token so a leaked old token can't outlive the change) + a `ChangePasswordPage` reachable from the student welcome page.
  - **Tokens never expired, logout didn't revoke them**: `logout()` previously only cleared `localStorage` — the token stayed valid forever server-side. Added `POST /api/logout/` (deletes the request's `Token`) and wired it into `logout()` as a fire-and-forget call before clearing local state (kept synchronous since every caller in the app already treats it that way).
  - **No rate limiting anywhere**: added `ScopedRateThrottle` to login (10/min), change-password (5/min), and mark-attendance (20/min) — tight limits on the two brute-forceable endpoints, looser on mark-attendance since legitimate retries after a duplicate/expired-QR rejection are normal.
  - **Secrets hardcoded in `settings.py` and committed to git**: `SECRET_KEY`, DB credentials, `DEBUG`, `ALLOWED_HOSTS`, `CORS_ALLOWED_ORIGINS` now read from environment via `python-dotenv` + a gitignored `backend/.env` (generated a fresh real `SECRET_KEY`, replacing the one that had been sitting in git history), with dev-safe fallback defaults so the app still runs with no `.env` present. Added `backend/.env.example` (checked in) as the template. Verified `manage.py check` and the full test suite still pass against the real MariaDB DB with this change.
  - **No weekly/monthly attendance view**: the user asked directly how to check this — answer was "you can't, only cumulative Analytics, single-day Day-wise, or a full-history export." Added `date_from`/`date_to` support to `get_closed_sessions()`/`build_attendance_matrix()` and threaded it through `AnalyticsView` and all three export views (shared `_parse_date_range()` helper). `AnalyticsPage` gained From/To date pickers plus "This Week" / "This Month" / "All Time" quick-preset buttons. Verified live: "This Week" correctly showed 0 sessions (none closed yet this week) vs "All Time" showing the real 3 — proving the filter actually changes results, not just decoration.
  - Flagged but did not fix (needs infrastructure, not just code): `CHANNEL_LAYERS` is `InMemoryChannelLayer`, so real-time updates break across multiple server processes — needs Redis (`channels_redis`) before any real multi-worker deployment.

  14 new backend tests (password change success/wrong-old/weak-new/token-rotation/unauthenticated, logout revokes token, login throttle triggers 429, analytics date-range scoping + invalid-format rejection). Full suite: 140/140 passing. Verified password-change and analytics-filter both live end-to-end in the browser (changed a test student's password, logged out, logged back in with the new one; toggled analytics presets and confirmed session counts actually changed).
- **2026-08-14** — Fixed a real usability gap the user caught: the standalone "Start Attendance" button/link (profile page + old nav shortcut) opened the form with no visible way to tell which section it was starting attendance for — no Section field existed at all, so outside timetable hours (when `getCurrentSchedule()` auto-fill has nothing to match) a teacher could start a session with a blank section and never know. Added a required Section `<select>` to `StartAttendancePage` (populated via `getAnalytics().available_sections`, same pattern as Day-wise), auto-filled the same way as before when a timetable slot or click-through prefill matches, but now always visible and always required — the placeholder "Select a section" plus a live "Only Section X students can mark this attendance" hint make it unambiguous either way. Verified live in-browser via the exact scenario reported: clicking the generic Start Attendance button now shows the dropdown, defaults to the placeholder, and picking "D" updates the hint correctly.
- **2026-08-14** — Live session roster + confirm-before-stop, both on `LiveQRPage`. **Roster**: added `get_session_roster(session)` — every student in the session's section, present/absent for THIS exact session (not "any session that day" like `get_day_attendance` — a deliberately different scope, since the live dashboard cares about the one session in progress; a second same-day session must not leak into it, covered by a dedicated test). `SessionLiveView` now returns it; `LiveQRPage` renders it below the QR/present-count row, defaulting to Absent, and flips a row to Present in real time off the same WebSocket `attendance.update` event already driving the "recent" list — no extra polling. **Confirm-before-stop**: "Stop Attendance" no longer closes the session immediately — it opens a modal ("Submit attendance?") showing the current present count, requiring an explicit "Submit Attendance" click; Cancel leaves the session running untouched. Verified both live in-browser: a real Section D session showed all 60 students defaulting to Absent with correct CRN/Roll No./Name, and clicking Stop opened the confirm modal instead of stopping. 3 new backend tests (roster scoping, present-status correctness, isolation from a second same-day session). Full suite: 129/129 passing.
- **2026-08-14** — Replaced Section B roster with corrected data. The user flagged the original `Data/Section B.csv` (95 students, imported 2026-08-14) as wrong and supplied the real one (`2026_08_14_crn_urn_name.csv`, 70 students). Confirmed scope with the user first since deleting DB records is irreversible (no backup/undo in this app) — deleted all 95 existing Section B `User`/`StudentProfile` records (0 `Attendance`/`ActivityLog` rows were attached, so no attendance history was lost), replaced `Data/Section B.csv` with the corrected file, and re-imported all 70 students with the current scheme (username=CRN, password=first4(name)+CRN-suffix, dedicated `urn` field, course=BBA, semester=3). All 6 sections now: A 67, B 70, C 63, D 60, E 64, F 66 — 390 students total. Full suite: 126/126 passing (data-only change, no code touched).
- **2026-08-14** — Live QR-refresh countdown, replacing the static "QR refreshes every 15 seconds" label. `LiveQRPage` now tracks `qrRefreshAt` (set to `Date.now() + 15000` each time a fresh token is fetched, both the initial load and every 15s refresh) and derives `qrSecondsLeft` each tick from the countdown timer already running for the attendance-window display — no new interval needed, reused the existing 1s ticker. Renders "QR refreshes in {n}s", ticking 15→14→13...→0 in real time, resetting cleanly on each refresh. Verified live in-browser via two Playwright screenshots ~3s apart: 15s → 12s, confirming it's a real countdown, not a static label.
- **2026-08-14** — Fixed a real data-loss bug caught by the user right after the credential-scheme change (below): `StudentProfile` never had its own URN field — the original import stored URN as `user.username`, so when the credential scheme changed `username` to CRN, the actual URN was silently overwritten and lost from the DB (Analytics/Day-wise "Roll No." columns started showing CRN duplicated instead). Added a proper `urn` field to `StudentProfile` (migration `0002`) and backfilled all 415 records from the original `Data/Section *.csv` files — the source CSVs still had the real URNs on disk, so nothing was actually unrecoverable, just misrouted in the DB. Verified against the user's own example: CRN `25BBA015` now correctly has `urn=25262101758`, distinct from `username=25BBA015`. Updated `AnalyticsView`/`get_day_attendance` to read `student_profile.urn` instead of `student.username` for the "Roll No." column; fixed the 2 tests that had baked in the old (wrong) assumption. Verified live via the running API. Full suite: 126/126 passing.
- **2026-08-14** — New student credential scheme: username=CRN, password=first 4 letters of first name (uppercase) + CRN with its 2-digit year prefix stripped — e.g. CRN `25BBA015`, name "Divyanshi Gautam" → username `25BBA015`, password `DIVYBBA015` (exact example the user gave; verified the migration script reproduces it precisely). Replaces the original scheme (username=password=URN/roll-number) across all 415 existing students — checked for collisions first (no CRN collided with an existing non-student username, no duplicate CRNs) before applying. Spot-checked via `authenticate()` and a live end-to-end login through the running API. Data-only change, no schema migration needed.
- **2026-08-14** — Removed open self-registration: all accounts are now pre-created (admin via Django admin, students via the CSV import script), so public sign-up no longer fits the access model — "no user can create an account without approval." Deleted `RegisterStudentView`, `StudentRegistrationSerializer`, the `/api/student/register/` route, and their tests on the backend; deleted `RegisterPage.tsx`, its `/register` route, the `registerStudent()`/`RegisterPayload` client code, and the "Register here" link on the login page on the frontend. Verified live: `POST /api/student/register/` now 404s, and the login page no longer offers a way in except a valid pre-created account. 126/126 backend tests pass (128 minus the 2 registration tests removed), typecheck clean.
- **2026-08-14** — Imported Section A (67), B (95), C (63) students from newly-added `Data/Section A.csv`, `B.csv`, `C.csv` — same scheme as the original D/E/F import (username=password=Roll No./URN, course=BBA, email=`{urn}@bba.local`). All 6 sections (A-F) now exist, 415 students total. Also corrected `semester` on all 190 pre-existing D/E/F records from `1` (my own placeholder guess during the original import, never verified) to `3`, matching the real timetable's "BBA **3rd** Semester" title — every `StudentProfile` now has `semester=3`. `Data/` stays gitignored (real student PII, never committed).
- **2026-08-14** — Two things: manual attendance override, and a real-world timezone bug the user caught live ("today is Friday 1:26am and it still shows Thursday"). **Timezone fix**: `settings.py` had `TIME_ZONE = 'UTC'`; at 1:26 AM IST the UTC clock is still ~7:56 PM the previous day, so every `timezone.localdate()`/`localtime()` call (today's-schedule matching, session `date` defaults, day-attendance lookups — all of it) was silently off by a day for hours every night. Changed to `'Asia/Kolkata'`; `USE_TZ` stays on so the DB still stores UTC internally, only the "what is local now" conversion changed. Verified live against the running server (was returning "Thursday", now correctly "Friday") and confirmed the full suite still passes — tests build datetimes via `timezone.make_aware()`, which is timezone-relative, so they stayed self-consistent across the change. **Manual override**: added `set_manual_attendance(session, student, present)` (get-or-create / delete the `Attendance` row directly — deliberately skips `ActivityLog`, since a teacher's own correction isn't a suspicious event, unlike a QR scan) and `POST /api/attendance/sessions/<id>/manual/` (`{crn, present}`, teacher-owns-session check, rejects a student from the wrong section same as QR scanning does). Wired into `DayAttendancePage`: the Present/Absent stamp is now a clickable toggle when there's exactly one session that day for that section (the normal case) — disabled with an explanatory note if there are 0 or 2+ sessions, to avoid ambiguity about which session gets corrected. While testing found and fixed a real display bug on the same page: the session-time badge was using `formatTime()` (built for `ClassSchedule`'s plain `"HH:MM:SS"` field) against `AttendanceSession.start_time`, which is a full ISO datetime — rendered as "NaN:03 AM". Replaced with a dedicated `formatSessionTime()`. Verified the full toggle round-trip live in-browser (click Absent→Present, count updates 0→1; click again, back to 0). 8 new backend tests for the override endpoint. Full suite: 128/128 passing.
- **2026-08-14** — Day-wise attendance view, in response to "how do I check day/date-wise attendance for a section" — previously the only day-level breakdown was buried in the CSV/Excel/PDF export columns, no on-screen view. Added `get_day_attendance(section, date)` service (every student in the section, present if they attended ANY of that section's sessions on that date — usually one, but stays correct if a teacher starts an extra ad-hoc session same day) and `GET /api/attendance/day/?section=&date=` (`DayAttendanceView`, teacher-only, 400s on missing/malformed params). New `DayAttendancePage` at `/teacher/day-attendance`: section + date pickers, present/absent table with S.No/CRN/Roll No./Name, "No session this day" badge when nothing was started. Also made the app's top nav `position: sticky` (was static, scrolled away) per direct request, and added the new "Day-wise" nav link alongside it. Verified live in-browser: page loads real Section D roster, correctly shows "No session this day" for today (none started yet) with all 60 students marked absent, and the sticky nav stays pinned while scrolling through the table. 8 new backend tests. Full suite: 120/120 passing.
- **2026-08-14** — Analytics table: student search, S.No, and Roll No. columns, plus a real bug fix hit while touching this code. Search box filters the already-fetched student list client-side by name or CRN (no extra API round trip); S.No is derived from the *filtered* array's index, not the original list, so it stays 1, 2, 3... correctly renumbered after a search narrows the results (verified live — searching "Pragya" against 190 students correctly renumbers to 1, 2, not the original row numbers). Added a Roll No. column using `student.username`, since that's the actual URN for every imported student (`StudentProfile` has no separate roll-number field — CRN and URN are different numbers on the real roster, and only CRN was shown before). While adding `roll_number` to `AnalyticsView`, noticed `overall_rate` was computed as `total_students * total_sessions` (a raw session **count**) instead of the periods-weighted total already used for every individual student's own percentage — so the school-wide rate silently drifted low whenever a merged double-period session existed, even though each student's own row was already correct. Fixed to weight by `sum(session.periods for session in sessions)`, matching `build_attendance_matrix()`. 2 new backend tests (roll number present, weighted overall-rate arithmetic on a fixture with one merged and one single-period session). Full suite: 112/112 passing.
- **2026-08-14** — Corrected the training subject: it's always "AI Training", not the generic "Training II" placeholder guessed earlier from the timetable image. Changed `ClassSchedule.subject`'s default, added a data migration (`0013`) renaming the 30 already-seeded rows, and made Start Attendance's subject field a fixed, disabled `"AI Training"` constant instead of free text — since it never varies, there's nothing for a teacher to type or typo, and it can't drift from what the timetable/reports expect. Backend itself stays flexible (any subject string is still accepted at the API level; only the UI fixed it), so this doesn't break the general-purpose `session-start` endpoint or its existing tests. Full suite: 110/110 passing.
- **2026-08-13** — Wrong-section attempts now actively notify the teacher, not just sit passively in the suspicious-activity panel. Previously `wrong_section` events (added in the section-lock fix above) only appeared if the teacher happened to be looking at the panel; the existing toast pattern was success-only ("X marked present"). Added a `toastVariant` ("success" | "warning") to `LiveQRPage` so the same toast mechanism now also fires — in real time over the existing WebSocket `activity.update` channel, no new polling — for wrong-section scans: `"{student} scanned this QR but isn't in Section {X}"`, shown longer (5s vs 3s) and in a warning color. `SessionLiveView` now also returns the session's own `section` so the toast can name the expected section. 1 new backend test confirming the WS broadcast fires for `wrong_section` the same way it already did for `expired_token`. Full suite: 110/110 passing.
- **2026-08-13** — Section lock on attendance marking, in response to the user asking what happens if a wrong-section student scans a section-specific QR — answer was "nothing, they're silently marked present," which is a real gap now that sessions carry a `section` (added for the clickable-timetable feature). Added `WrongSectionError` + `ActivityLog.TYPE_WRONG_SECTION`; `mark_attendance()` now rejects with `"This QR code is for Section A only."` when the scanning student's `StudentProfile.section` doesn't match `session.section` — checked right after the session-status check, before token-expiry/duplicate checks, since being the wrong student for this session is more fundamental than token freshness. Sessions with no section set (started without picking a timetable slot) skip the check entirely — deliberate backward-compat escape hatch, not a loophole, since those sessions were never section-scoped in the first place. Surfaced on the teacher's live suspicious-activity panel as "Wrong section". 3 new tests (wrong section rejected + logged, correct section allowed, section-less session still open to anyone). Full suite: 109/109 passing.
- **2026-08-13** — Clickable timetable: teacher's welcome-page timetable rows are now actionable, not just informational. Added a `section` field to `AttendanceSession` (migration `0010`) so a started session can be reliably matched back to the timetable slot it came from (subject text is free-form and teacher-editable, so matching on it wasn't safe — section is stable and, per the real BBA III timetable, unique per day). `get_today_sessions_by_section(teacher)` looks up today's sessions keyed by section; `TodayScheduleView` now annotates each merged block with `session_id`/`session_status` (only for that request's own user — a student never sees another user's session, and gets nulls throughout since students don't have sessions). Clicking a row with no session navigates to Start Attendance pre-filled (subject, section, and the merge checkbox pre-checked for double periods) via router state; clicking a row that already has a session jumps straight to its live view (`LiveQRPage`) — "View live" while active, "View attendance" once closed — so a teacher can see who's marked present without hunting through Analytics. Verified the full loop live in-browser via Playwright: click unscheduled slot → prefilled Start Attendance form (merge box correctly pre-checked for the D double period) → start session → QR/live view → back to welcome page → click same slot again → lands back on the *same* running session (countdown continued, not reset). 8 new backend tests (session-linking by section, isolation between teachers). Full suite: 106/106 passing.
- **2026-08-13** — Merge-attendance option for double periods: previously merging was display-only (Agent.md entry above) — a teacher still had to run two separate QR sessions for a continuous double period, so a student had to scan twice. Added a `periods` field (1 or 2) to `AttendanceSession` (migration `0009`), exposed on `StartSessionSerializer`/`SessionSerializer`. `StartAttendancePage` gained a "Merge with next continuous period" checkbox — checking it sends `periods: 2`, so one QR session and one scan covers both timetable slots. Attendance weighting updated everywhere percentages are computed so a merged session counts as 2 towards both present and total: `build_attendance_matrix()` (teacher analytics/exports) and `StudentHistoryView` (student's own history) now sum `session.periods` instead of counting sessions 1-for-1 — kept both in sync since they'd silently diverge otherwise. CSV/Excel/PDF export headers mark merged sessions as `"YYYY-MM-DD (x2)"` so a teacher reading the raw export isn't confused by the weight. 6 new tests (start with `periods=2`, reject `periods=3`, merged-session doubles both numerator and denominator in the matrix). Full suite: 103/103 passing.
- **2026-08-13** — Merge back-to-back same-section timetable periods into one display block, per user pointing out that consecutive slots for the same section (e.g. 10:05-10:55 and 11:05-11:55, both BBA III D) are really one continuous double-period class. Added `merge_consecutive_slots()` in `services.py`: walks the day's slots in order, folding a slot into the previous block when section+subject match — deliberately merges across the real timetable's short between-period break (10:55 → 11:05) rather than requiring an exact zero-gap boundary, since that's what the actual printed schedule looks like. `TodayScheduleView` now returns merged blocks, so both welcome-page timetable cards show "10:05 AM - 11:55 AM" once instead of two rows. 5 new tests (merge across a gap, no merge across a different section, no merge for different section back-to-back, full real Thursday schedule collapsing 6 rows to 3). Full suite: 99/99 passing.
- **2026-08-13** — Today's timetable on both welcome pages: added `get_today_schedule()` service + `GET /api/attendance/schedule/today/` (any authenticated role) returning all of today's slots in order, plus the weekday name. Teacher's welcome page (`TeacherProfilePage`) shows every slot for today across all sections; student's welcome page (`StudentProfilePage`) shows the same feed filtered client-side to the student's own `section`. Both cards are silent-fail (timetable is a convenience, not required for the rest of the dashboard to work) and hide entirely if there's nothing to show. New `formatTime()` util renders the backend's `"HH:MM:SS"` `TimeField` strings as a 12-hour clock. Verified live in-browser via Playwright screenshot on the actual running dev servers (not just tests) — logging in as `admin` on the real current day (Thursday) correctly showed only the D/D/E/E/F/F Thursday slots; logging in as a Section D student correctly showed only that section's 2 Thursday slots. 3 new backend tests (today-schedule service ordering, teacher sees all sections, student can access too). Full suite: 94/94 passing.
- **2026-08-13** — Timetable auto-fill for Start Attendance: added a `ClassSchedule` model (day of week, start/end time, section, subject) seeded via data migration with the real BBA III (Training II) weekly timetable from the printed schedule — 30 slots across Mon-Fri, sections A-F, Lab 3E. New `GET /api/attendance/schedule/current/` (teacher-only) resolves the slot covering the current local time via `get_current_schedule_slot()`; returns `{matched: false}` outside any slot (end time is exclusive, so back-to-back slots don't double-match). `StartAttendancePage` calls it on mount and pre-fills the subject field (`"Training II — BBA III D"`) with a visible "Auto-filled from today's timetable" hint when matched; silently no-ops otherwise since it's a convenience, not a requirement. `ClassSchedule` registered in Django admin for manual edits if the timetable changes next semester. 6 new tests (slot-matching edge cases including the exclusive end-time boundary, view auth, student-forbidden). Full suite: 91/91 passing.
- **2026-08-13** — Switched DB engine from SQLite to MySQL-family per explicit user request (`DB_NAME=classpulse, DB_USER=root, DB_PASSWORD=`). Discovered two local MySQL-compatible services already installed: a XAMPP-bundled MariaDB 10.4.32 (port 3306, root/no-password — matched the requested credentials exactly) and a standalone MySQL 8.0 (port 3307, root password set). Django 5.2's MySQL backend hard-rejects anything below MariaDB 10.5 (`NotSupportedError` at connection time), so 10.4.32 was unusable as-is. Rather than touch the XAMPP install in place (risk to other XAMPP-dependent projects) or reset MySQL 8.0's root password (declined), installed a separate MariaDB 12.3.2 via `winget install MariaDB.Server`, registered it as its own Windows service (`MariaDB`) on port 3306 — XAMPP's `mysql` service stopped (not removed) to free the port. Root account needed an explicit `ALTER USER ... IDENTIFIED VIA mysql_native_password USING ''` — the installer's default empty-plugin/empty-password root account authenticated fine via the `mysql` CLI but was rejected by PyMySQL's auth handshake. Added `pymysql.install_as_MySQLdb()` shim to `backend/classpulse/__init__.py` (PyMySQL chosen over `mysqlclient` to avoid needing a C build toolchain on Windows), pointed `DATABASES` in `settings.py` at `classpulse` (utf8mb4/utf8mb4_unicode_ci) on `127.0.0.1:3306`. Fresh-start migration (per user's choice, not a data migration): ran `migrate` clean, recreated the `admin` superuser (`Admin@1234`, role=teacher), re-ran the CSV import for the 190 students from `Data/Section D/E/F.csv` (same scheme as the original import: username=password=URN, course=BBA, semester=1, section per file). Full suite: 85/85 passing against MariaDB. Added `PyMySQL==1.2.0` to `requirements.txt`. Old `db.sqlite3` left on disk, untouched and unused.
- **2026-08-11** — Phase 4 Task 7 done (final task): suspicious-activity panel on `LiveQRPage` — initial REST fetch + live WebSocket feed, discriminated from attendance events via a `kind` field. Spec review confirmed none of this file's 3 prior review-caught bug classes (unhandled poll failure, stale error, dead WS with no feedback) were regressed. Code-quality review caught a NEW instance of the same "does every path degrade gracefully" failure class: an unmapped `activity_type` from the backend would crash the whole dashboard render (`ACTIVITY_LABELS[type]` → `undefined` → property access throws), and a failed activity fetch was silently swallowed — on a *security* panel, an empty list reading as "checked, nothing found" instead of "couldn't check" is actively misleading. Fixed both: defensive fallback label, and a visible warning on fetch failure. **Phase 4 (Security & Suspicious Activity) complete** — all 7 tasks done, exit criteria met (every attempt logged with IP/device, duplicate/expired attempts visible to teacher near-real-time, new-device heuristic in place, Phase 2's DB-level duplicate/expiry enforcement untouched).
- **2026-08-11** — Phase 4 Task 6 done: `ActivityLog` registered in admin, backend sanity check — 60/60 tests passing, `manage.py check` clean. Backend half of Phase 4 complete (Tasks 1-6) — moving to frontend (Task 7).
- **2026-08-11** — Phase 4 Task 5 done: `GET /api/attendance/sessions/<id>/activity/` (teacher-only, ownership-scoped) — last 50 non-success activity entries, for initial dashboard load. Spec + code-quality reviewed, approved. Fixed one gap before merge: relied on `ActivityLog.Meta.ordering` implicitly instead of an explicit `.order_by()` (would silently break if that default ever changed) — made explicit, matching `SessionLiveView`'s established style. Noted, not fixed: no test locks down the 50-row cap itself.
- **2026-08-11** — Phase 4 Task 4 done: `activity_update` consumer handler relays suspicious-activity broadcasts to connected teacher dashboards — closes a gap that existed since Task 2 (broadcasts were being sent since then but had no handler to receive them on a real, non-mocked connection). Verified directly (exact-match spec, 3-line additive diff).
- **2026-08-11** — Phase 4 Task 3 done: new-device detection — logs `TYPE_NEW_DEVICE` (non-blocking) when a student's User-Agent changes between successful marks. Spec + code-quality reviewed, approved. Flagged for later (not fixed, both non-blocking): exact-string UA comparison will likely false-positive often in real use (browser auto-updates, extensions) — fine for a v1 "log it" heuristic, but shouldn't drive any real alert/notification without normalization first; no DB index on `(student, activity_type, created_at)` for this now-per-mark query, negligible at current scale.
- **2026-08-11** — Phase 4 Task 2 done: mark-attendance validation moved from Phase 2's DRF serializer into `services.py::mark_attendance()`, exception-driven (5 typed exceptions from Task 1), every branch logs an `ActivityLog` row before failing/succeeding. Spec + code-quality reviewed, approved. Fixed one real gap before merge: `log_activity()`'s DB write was unguarded — a transient failure there could turn an already-successful, already-committed attendance mark into a 500 for the student (same failure class as the Phase 3 broadcast fix); wrapped in try/except, logs and continues. Noted, not fixed: double duplicate-check (pre-check + IntegrityError backstop) is intentional TOCTOU-safe redundancy but undocumented; exception classes couple internal `activity_type` with user-facing `message` on one attribute set.
- **2026-08-12** — Phase 5 Task 9 done (final task): teacher `AnalyticsPage` — totals, overall rate, per-student table, below-threshold warning, CSV/Excel/PDF export buttons via blob download. Spec + code-quality reviewed. Needs-changes on first pass, both real: (1) export buttons had no disabled/in-progress state, so a double-click could fire two concurrent downloads with no feedback; (2) the 75% threshold was now hardcoded independently in 3 places (backend `AnalyticsView`, `AttendanceHistoryPage`, this page) — the "fix on third occurrence" bar flagged in an earlier review had been hit. Fixed: added a `downloadingFormat` guard disabling all three buttons mid-download; extracted `ATTENDANCE_THRESHOLD` into `api/client.ts`, used by both frontend pages (backend's own `75` literal in `AnalyticsView` still separate — cross-language, no shared-constant mechanism exists). Also hardened `revokeObjectURL` timing for Safari/WebKit (delayed via `setTimeout` instead of firing synchronously after `click()`). Re-verified directly, approved.

- **2026-08-13** — Two feature additions on user request. (1) Section filter on teacher analytics + exports: `build_attendance_matrix(section=)` now optionally scopes students; `AnalyticsView`/`ExportCSVView`/`ExportExcelView`/`ExportPDFView` all accept `?section=`; `available_sections` + current `section` echoed in the analytics response for the frontend dropdown; export filenames include the section when filtered. (2) Configurable attendance-window duration (doc.md §12): `AttendanceSession.duration_minutes` (default 5, 1-180) + `closes_at` computed property; `close_session_if_window_expired()` lazily auto-closes on the next touch (mark attendance, QR fetch, live poll) — same no-scheduler-needed pattern as QR-token rotation. Teacher picks the window on Start Attendance; live dashboard shows a client-side countdown against `closes_at`. Also: bulk-imported 190 real students from user-supplied section CSVs (D/E/F, BBA sem 1, username=password=URN) via a one-off script, not committed to git (`Data/` added to `.gitignore` — PII); added `list_display`/`list_filter`/`search_fields` to Django admin's `StudentProfile` registration so section-wise lookup doesn't require a script; fixed a real crash (`StudentProfile.DoesNotExist` → unhandled 500, flagged back in Phase 1 and never fixed) triggered when the `admin` Django-admin-only account was used to log into the app — now a clean 404, with a regression test.
- **2026-08-13** — Full visual redesign, all 9 pages. Root cause of the reported invisible-login-heading bug: `index.css` was still the unmodified Vite scaffold template — `color-scheme: light dark` + a dark-mode `prefers-color-scheme` variant set heading color near-white, invisible on Bootstrap's white background. Replaced entirely with a real design system: chalkboard-green + brass + paper-cream palette, Space Grotesk/Inter/IBM Plex Mono, a "stamp" badge as the signature element (present/absent/status, styled as a tilted rubber ink-stamp — fits the attendance-register subject directly) replacing every plain Bootstrap Badge. Added `AppShell` — a shared chalkboard header with role-aware nav and a working Log Out control, which did not exist anywhere in the UI before this. Wired in the user-provided ClassPulse logo (resized 1254px→256px, 628KB→52KB, since it only ever renders at 40-64px). `LiveQRPage`/`AnalyticsPage` got visual-only changes — every state/effect/handler left byte-for-byte untouched given this file's multi-round bug history. Picked up two small fixes along the way: `controlId` on all form groups (a11y gap flagged back in Phase 1, never fixed), semester input min/max matching backend validation. Verified via Playwright screenshots against live servers (login, register, teacher profile+shell, analytics) — all render correctly. Backend untouched, 77/77 still passing.
- **2026-08-13** — Manual E2E verification run: real browser (Playwright/Chromium), both dev servers live, full flow — student register → login → teacher login → start session → live QR → mark attendance (via API, camera unavailable headless) → real-time WebSocket update on teacher dashboard (toast + present count + recent list, confirmed via screenshot) → stop session → student history page (correct 100%, closed-sessions-only) → teacher analytics page (correct aggregation) → CSV export download triggered successfully. Zero console/page errors across all 7 pages visited. **Nothing broken found** — this was the first real click-through since the whole build; every phase held up. Test accounts and scratch script cleaned up afterward; `playwright` devDependency added only for this run, reverted (not part of the app's actual stack).

**🎉 ClassPulse v1 complete.** All 5 phases from `docs/plan.md`'s Development Plan are built, reviewed, and committed: Authentication → Attendance Sessions & QR → Real-Time Dashboard → Security & Suspicious Activity → Reports & Analytics. Every task went through two-stage review (spec compliance + code quality); real bugs were caught and fixed at nearly every stage, several more than once on the same file (`LiveQRPage.tsx`: 4 rounds; PDF export column math: 2 rounds). Backend: 79 tests passing. Frontend: builds clean. Not yet done: a manual end-to-end browser walkthrough (register → login → start session → scan → live dashboard → close session → history/analytics/export) — only automated tests and build checks have been run so far.
- **2026-08-12** — Phase 5 Task 8 done: student `AttendanceHistoryPage` — total/present/percentage badge + full history table, linked from `StudentProfilePage`. Spec + code-quality reviewed, approved. Flagged (not fixed, explicit follow-up): the fetch-failure pattern (logout + redirect on ANY error, not just 401/403) is now duplicated across 3 pages (student profile, teacher profile, this one) — a transient network blip destroys a valid session same as an auth failure. Worth a shared `handleAuthOrRedirect()` helper before a 4th page copies it. Minor, not fixed: no "no history yet" empty state for brand-new students.
- **2026-08-12** — Phase 5 Task 7 done: backend sanity check — 77/77 tests passing, `manage.py check` clean. Backend half of Phase 5 complete (Tasks 1-7: matrix service, student history, teacher analytics, CSV/Excel/PDF export) — moving to frontend (Tasks 8-9).
- **2026-08-12** — Phase 5 Task 6 done: `GET /api/attendance/export/pdf/` (teacher-only) — final export format, via `reportlab`, reusing `build_report_rows()`. Code-quality review reproduced 3 real defects, all verified with actual numbers: (1) unconstrained `Table` overflowed the page ~4.7x at realistic scale (45 students × 60 sessions), silently clipping most of the report; (2) reusing the CSV/Excel sanitizer's quote-prefix in PDF showed a stray literal apostrophe (PDF isn't a spreadsheet, that convention doesn't apply); (3) no `repeatRows`, so multi-page reports lost headers after page 1. First fix pass addressed (2) and (3) correctly but got (1) wrong — margin miscalculated (off by 72pt) and a "minimum floor" that itself pushed total width past the page once column count passed ~24, reproduced concretely by the reviewer down to specific overflow ratios; the fix's own weak test (`len(pages) >= 1`) couldn't have caught this since reportlab clips silently rather than erroring. Second pass: corrected the margin, removed the overflow-causing floor (columns now shrink toward unreadable rather than ever exceeding page width), extracted the width math into a standalone `compute_pdf_column_widths()` and tested the actual invariant directly (`sum(widths) <= usable_width`, parametrized up to 200 columns) instead of trying to infer it from rendered PDF geometry. Verified directly this time, not re-reviewed by agent — math is transparent and unit-tested.
- **2026-08-12** — Phase 5 Task 5 done: `GET /api/attendance/export/excel/` (teacher-only) — XLSX via `openpyxl`, deliberately deviated from the plan's original inline code to reuse `build_report_rows()` instead, so it inherits the CSV export's formula-injection sanitization for free rather than re-duplicating it. Spec + code-quality reviewed, approved. Added an Excel-specific sanitization regression test to close a coverage gap the review flagged (CSV had one, Excel didn't). Noted, not fixed (cosmetic only): `openpyxl` has no native "quote prefix" cell attribute, so the sanitizing `'` prefix is safe (never evaluated as a formula) but displays as a literal leading apostrophe in Excel, unlike CSV where spreadsheet apps typically absorb it on import.
- **2026-08-12** — Phase 5 Task 4 done: `GET /api/attendance/export/csv/` (teacher-only) — downloadable CRN×Name×session-dates×% matrix. Code-quality review found a real security gap: CRN/name are free-text at student self-registration, and were written unescaped into CSV cells — a student registering with a name/CRN starting with `=`/`+`/`-`/`@` gets formula-injected into any teacher who opens the export in Excel/Sheets (CWE-1236). Fixed with `sanitize_report_cell()` (quote-prefix guard), extracted alongside a shared `build_report_rows()` helper so the fix — and the row-building logic itself — doesn't get re-duplicated when Excel/PDF exports land in Tasks 5-6. Also added a UTF-8 BOM so Excel renders non-ASCII student names correctly, and a regression test locking down the sanitization. Re-reviewed, approved.
- **2026-08-12** — Phase 5 Task 3 done: `GET /api/attendance/analytics/` (teacher-only) — overall rate, per-student breakdown, below-threshold list. Spec + code-quality reviewed, approved. Fixed: `overall_rate` had already redundantly duplicated the percentage formula inline instead of using `attendance_percentage()` extracted one commit earlier — deduped. Noted, not fixed: `75` threshold is a bare literal (fine for v1, worth a named constant if it ever becomes configurable); endpoint has no per-teacher ownership scoping — deliberate v1 limitation (single-class scope, no course model to scope by), documented as such, would need revisiting for multi-teacher support.
- **2026-08-12** — Phase 5 Task 2 done: `GET /api/attendance/student/history/` — student's own history + percentage, closed sessions only. Implementer caught and fixed a real bug in this plan's own test fixture (two sessions sharing a subject broke a dict-keyed assertion) — verified minimal and correctly scoped to the test, not the view. Spec + code-quality reviewed, approved. Fixed proactively: extracted `attendance_percentage()` shared helper (percentage formula was independently duplicated in `views.py` and `services.py`, would have become a third duplicate at Task 3/analytics) — same formula, one place now.
- **2026-08-12** — Phase 5 (Reports & Analytics) started. Wrote detailed plan: `docs/superpowers/plans/2026-08-12-phase-5-reports.md` (9 tasks). Task 1 done: shared `build_attendance_matrix()` service — closed sessions × students × present/absent grid, reused by student history, teacher analytics, and CSV/Excel/PDF exports. Spec + code-quality reviewed, approved. Fixed proactively (not a bug, a maintainability call): return value changed from a bare `(sessions, rows)` tuple to a `NamedTuple` before 5 planned callers land — same unpacking syntax, more self-documenting. Flagged, not fixed: CRN sort is lexicographic not numeric (fine if CRNs are zero-padded in practice; worth checking real data before it flows into report exports).
- **2026-08-11** — Phase 4 (Security & Suspicious Activity) started. Wrote detailed plan: `docs/superpowers/plans/2026-08-11-phase-4-security.md` (7 tasks). Task 1 done: `ActivityLog` model (audits every attendance attempt) + `attendance/exceptions.py` (5 typed failure classes, wired up in Task 2). Spec + code-quality reviewed, approved; fixed one real gap before it went live: `session` FK was `CASCADE` (deleting a session would wipe its own audit trail) — changed to `SET_NULL` so log entries survive. Also noted for later, not fixed: `AttendanceError` base class doubles as a usable concrete exception (should be raised only via subclasses; harmless today since nothing raises the base directly).
- **2026-08-11** — Phase 3 Task 8 done (final task): `LiveQRPage` extended with present-count badge, recent list, toast notifications, live-driven by `connectToAttendanceSocket`. Code-quality review found the exact "silent stall, no feedback" pattern this file was already fixed for twice in Phase 2 — reintroduced via unhandled `getSessionLive` rejection and a WebSocket with no error/close handling or reconnect; fixed with `.catch()`, `onerror`/`onclose`/auto-reconnect in `ws.ts`, a status badge, and a stable per-event toast key (also found + fixed: unstable list `key`, and a toast-autohide-timer race where same-named students could dismiss each other's toast early). Re-review caught the toast key fix was incomplete (message-text key still collided for same-named students); tightened to a monotonic counter. Re-verified directly, approved. **Phase 3 (Real-Time Dashboard) complete** — all 8 tasks done, exit criteria met (WebSocket push updates dashboard with no refresh, auth+ownership scoped per session, QR rotation unaffected). Known accepted tradeoff: WebSocket reconnects forever with no backoff/cap if the endpoint is fundamentally broken — flagged as non-blocking follow-up for Phase 4+.
- **2026-08-11** — Phase 3 Task 7 done: frontend `getSessionLive` REST call + `connectToAttendanceSocket` native WebSocket client (token via query string, matches localStorage token). Verified directly (exact-match spec, pure additions).
- **2026-08-11** — Phase 3 Task 6 done: backend sanity check — 49/49 tests passing, `manage.py check` clean, dev server confirmed serving via daphne (`Server: daphne` header, not plain WSGI dev server). Backend half of Phase 3 complete (Tasks 1-6) — moving to frontend (Tasks 7-8).
- **2026-08-11** — Phase 3 Task 5 done: `GET /api/attendance/sessions/<id>/live/` (teacher-only, ownership-scoped) — present count + last-10 recent list, for initial dashboard load before WS events arrive. Spec + code-quality reviewed, approved. Noted for frontend Task 8: WS event payload and REST `recent` items share `{name, crn, marked_at}` shape but `present_count` nests differently (per-event in WS vs. top-level in REST) — plan's Task 8 code already handles this correctly by destructuring explicitly, not reusing types wholesale.
- **2026-08-11** — Phase 3 Task 4 done: `broadcast_attendance_update` called from `MarkAttendanceView` after successful mark, pushes `{name, crn, marked_at, present_count}` to session's WS group. Code-quality review found reliability gap: unguarded `group_send` could turn a successful, already-committed DB write into a 500 for the student; fixed with try/except + logging (never masks a successful mark), also tidied mid-file imports. Re-verified directly, approved.
- **2026-08-11** — Phase 3 Task 3 done: `AttendanceConsumer` — teacher-only, ownership-checked (close 4403/4404), joins `attendance_session_<id>` group. Code-quality review found fragile `disconnect()` guard (`hasattr(self, "group_name")` doesn't actually track join state, since `group_name` is set before auth checks run); fixed with explicit `joined_group` flag. Re-verified directly, approved.
- **2026-08-11** — Phase 3 Task 2 done: `TokenAuthMiddleware` (WebSocket auth, resolves `?token=` against the existing DRF `authtoken.Token`, sets `scope["user"]`). Code-quality review found deactivated users could still authenticate over WS (no `is_active` check, unlike DRF's own `TokenAuthentication`); fixed with `user__is_active=True` filter. Re-verified directly, approved.
- **2026-08-11** — Phase 3 (Real-Time Dashboard) started. Wrote detailed plan: `docs/superpowers/plans/2026-08-11-phase-3-realtime-dashboard.md` (8 tasks). Task 1 done: Django Channels scaffolded (`daphne`+`channels` in `INSTALLED_APPS`, in-memory channel layer, `asgi.py` rewritten). Spec + code-quality reviewed, approved; added a one-line settings comment on the in-memory layer's single-process constraint per review.
- **2026-08-11** — Phase 2 Task 9 done (final task): student `ScanQRPage` (camera scan via `html5-qrcode`, auto-submits to `markAttendance`), linked from `StudentProfilePage`. Code-quality review found Critical bug (`scanner.stop()` throws synchronously, not via rejected promise, if camera permission was denied — escaped the `.catch()` guard uncaught) plus the same missing-mounted-guard pattern already fixed twice on `LiveQRPage`; fixed with `getState() === SCANNING` check before stop + `active` flag. Re-reviewed, approved. **Phase 2 (Attendance Sessions & QR) complete** — all 9 tasks done, exit criteria met (QR rotates 15s, one scan = one mark, DB-level duplicate prevention, all validation server-side).
- **2026-08-11** — Phase 2 Task 8 done: teacher `StartAttendancePage` + `LiveQRPage` (polls QR every 15s via `qrcode.react`), linked from `TeacherProfilePage`. Code-quality review found two Critical bugs repeating a Phase-1-fixed pattern (unhandled poll failure → stuck spinner forever; unhandled stop failure → button silently no-ops) — fixed with error state + unconditional navigate-on-stop. Re-review caught a third bug in the fix itself (error state never cleared on recovery, permanently hiding QR after one transient blip) — fixed with `setError(null)` on successful poll. Verified directly, approved.
- **2026-08-11** — Phase 2 Task 7 done: frontend API client extended (`startSession`, `stopSession`, `getSessionQR`, `markAttendance`). Verified directly (exact-match spec, pure append) — no separate review dispatch needed for this mechanical task.
- **2026-08-11** — Phase 2 Task 6 done: attendance models registered in admin. Full backend suite: 35/35 passing. Backend half of Phase 2 complete (Tasks 1-6) — moving to frontend (Tasks 7-9).
- **2026-08-11** — Phase 2 Task 5 done: `POST /api/attendance/mark/` — core feature, full server-side validation chain (token exists → session active → not expired → not already marked → student role via `IsStudent`), student-only, DB-level `UniqueConstraint` backstop. Code-quality review found Critical bug: unhandled `IntegrityError` under concurrent duplicate-scan race → raw 500 instead of clean 400; fixed with `transaction.atomic()` + `except IntegrityError` in the view. Re-reviewed, approved. Follow-up nice-to-have (not blocking): a mocked test exercising the `except IntegrityError` branch directly, vs. current code-inspection-level confidence — true threading-based race test out of scope.
- **2026-08-11** — Phase 2 Task 4 done: `GET /api/attendance/sessions/<id>/qr/` (teacher-only, ownership-scoped), lazy 15s rotation via `attendance/services.py::get_current_qr_token`. Code-quality review found non-deterministic tie in token ordering (`order_by("-created_at")` alone); fixed with `-id` secondary sort. Confirmed benign, accepted TOCTOU race on concurrent-request duplicate-mint (no security/correctness impact — validation at scan time is fully decoupled from issuance, per `MarkAttendanceSerializer` design in Task 5).
- **2026-08-11** — Phase 2 Task 3 done: teacher-only `POST /api/attendance/sessions/start/` and `.../stop/` (ownership-scoped, other teachers' sessions 404). Code-quality review found repeat-stop-call bug (`end_time` silently overwritten on each call); fixed with an already-closed guard returning 400. Re-reviewed, approved.
- **2026-08-11** — Phase 2 Task 2 done: `attendance` app scaffolded, `AttendanceSession`/`QRToken`/`Attendance` models (DB-level `UniqueConstraint` on student+session). Code-quality review found latent bug (`QRToken.expires_at` NOT NULL but only set in `save()` override — bypassable via bulk_create/full_clean); fixed by switching to `default=` callable, matching the `token` field's existing pattern. Re-reviewed, approved.
- **2026-08-11** — Phase 2 (Attendance Sessions & QR) started. Wrote detailed plan: `docs/superpowers/plans/2026-08-11-phase-2-attendance-qr.md` (9 tasks). Task 1 done: `IsTeacher`/`IsStudent` DRF permission classes (`accounts/permissions.py`) — closes the role-gating gap flagged at end of Phase 1. Spec + code-quality reviewed, approved. Note: permission tests exercise `has_permission` directly rather than through a real DRF request/view cycle — acceptable, integration coverage arrives naturally once Task 3+ wires these into `attendance` views.
- **2026-08-10** — Phase 1 Task 9 done (final task): teacher profile endpoint (`GET /api/teacher/profile/`) + `TeacherProfilePage`, symmetric to student profile. Spec + code-quality reviewed, approved. **Phase 1 (Authentication & Profiles) complete** — all 9 tasks done, exit criteria met (student register/login/profile, teacher login/profile, DB-level unique CRN). Cross-cutting gap flagged for Phase 2: neither profile endpoint checks `request.user.role` — a student token can fetch `/api/teacher/profile/` and vice versa, no error, just wrong-shaped-but-valid data. Not blocking Phase 1, becomes load-bearing once Phase 2 adds teacher-only session endpoints.
- **2026-08-10** — Phase 1 Task 8 done: registration/login/student-profile pages + route guard (`ProtectedRoute`), wired in App.tsx. Code-quality review found unhandled-401 stuck-spinner bug + blank page on unmatched route (teacher login pre-Task-9); fixed (StudentProfilePage now logs out + redirects on fetch failure, added catch-all route → `/login`). Re-reviewed, approved. Remaining minor/non-blocking: RegisterPage's narrow error-message fallback, missing `controlId` on form groups (a11y), no visually-hidden spinner text.
- **2026-08-10** — Phase 1 Task 7 done: frontend API client (`frontend/src/api/client.ts`) — register/login/profile calls, token interceptor. Spec + code-quality reviewed, approved. Notes for later: type responses on `registerStudent`/`getStudentProfile` (currently `any`), move `BASE_URL` to env var before deploy, no server-side token invalidation on logout.
- **2026-08-10** — Phase 1 Task 6 done: React+Vite+TS frontend scaffolded (`frontend/`), Bootstrap 5 + react-bootstrap + react-router-dom + axios installed. Spec + code-quality reviewed, approved. Note: scaffold resolved React 19 (plan said React 18) — non-breaking, watch for API differences in later tasks.
- **2026-08-10** — Phase 1 Task 5 done: student profile endpoint (`GET /api/student/profile/`), admin registration for User+StudentProfile. Spec + code-quality reviewed, approved with follow-ups: (1) `StudentProfile.DoesNotExist` → unhandled 500 if a teacher or profile-less user hits this route, should be 403/404 — fix before frontend relies on it; (2) `photo` serializes as relative path, not absolute URL (no `context=request` passed); (3) mid-file imports in views.py, cosmetic.
- **2026-08-10** — Phase 1 Task 4 done: role-aware token login (`POST /api/student/login/`, returns token+role+username). Spec + code-quality reviewed, approved. Noted for later phase: no logout/token-revocation endpoint, no throttling.
- **2026-08-10** — Phase 1 Task 3 done: student registration endpoint (`POST /api/student/register/`), DB-level unique CRN + username enforced. Spec + code-quality reviewed, approved. Noted for later: response omits `photo` field, minor TOCTOU race on uniqueness checks under concurrent requests (plan-level tradeoff, not blocking).
- **2026-08-10** — Phase 1 Task 2 done: custom `User` model (role field) + `StudentProfile` (unique CRN) in `accounts` app, migration + 3 model tests passing. Spec + code-quality reviewed, approved.
- **2026-08-10** — Phase 1 Task 1 done: Django backend scaffolded (`backend/classpulse`, `backend/accounts`), DRF token auth + CORS + media settings wired. Spec + code-quality reviewed, approved. Added root `.gitignore`.
- **2026-08-10** — Wrote detailed Phase 1 (Auth) implementation plan: `docs/superpowers/plans/2026-08-10-phase-1-auth.md`. TDD task-by-task, backend (Django+DRF token auth) and frontend (React+Bootstrap). Not yet executed.
- **2026-08-10** — Stack finalized: decoupled Django REST/Channels backend + React+Bootstrap frontend (`backend/` + `frontend/`). Updated `Agent.md`, `docs/plan.md`, `Readme.md`.
- **2026-08-10** — Repo initialized. Wrote `document.md` spec (pre-existing), `docs/plan.md` (5-phase plan), `Agent.md`, `Readme.md`. No code yet.
